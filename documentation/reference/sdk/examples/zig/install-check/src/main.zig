// The executable `install-check/build.zig` wires the client into. What it does past that is
// nothing this project asks about — the "Installation" example is a build script, not application
// code, and what it proves is that the dependency resolves and the import compiles.
pub fn main() void {}
