<%
if (locals.i18nHelpers) { Object.assign(locals, locals.i18nHelpers.getPageLocals('privacy-policy', locals.lang || 'en')); }
var t = locals.t;
%><!DOCTYPE html>
<html lang="<%= locals.pageLang %>">
<head>
    <title><%= locals.pageTitle %></title>
    <%- include('./includes/_head.ejs') %>
    <style>
        .policy-table {
            width: 100%;
            border-collapse: collapse;
            margin: 1.5rem 0;
        }
        .policy-table th,
        .policy-table td {
            border: 1px solid rgba(255, 255, 255, 0.1);
            padding: 0.875rem 1rem;
            text-align: left;
            vertical-align: top;
        }
        .policy-table th {
            background-color: rgba(99, 102, 241, 0.3);
            color: white;
            font-weight: 600;
            font-size: 0.875rem;
            text-transform: uppercase;
            letter-spacing: 0.05em;
        }
        .policy-table td {
            background-color: rgba(255, 255, 255, 0.02);
            color: #d1d5db;
            font-size: 0.9375rem;
            line-height: 1.5;
        }
        .policy-table tr:hover td {
            background-color: rgba(255, 255, 255, 0.05);
        }
        .section-anchor {
            scroll-margin-top: 5rem;
        }
    </style>
</head>
<body>
<%- include('./includes/_header.ejs') %>

<!-- Hero Section -->
<section class="relative gradient-hero overflow-hidden py-20 sm:py-24">
    <div class="absolute inset-0 grid-pattern opacity-50"></div>
    <div class="blur-circle blur-circle--indigo w-96 h-96 -top-48 -left-48"></div>
    <div class="blur-circle blur-circle--green w-80 h-80 top-1/2 -right-40"></div>

    <div class="relative z-10 max-w-4xl mx-auto px-4 sm:px-6 lg:px-8 text-center">
        <span class="section__eyebrow"><%= t.hero.eyebrow %></span>
        <h1 class="mt-4 text-display-xl text-white">
            <%= t.hero.title %>
        </h1>
        <p class="mt-6 text-body-lg text-gray-400 max-w-2xl mx-auto">
            <%= t.hero.subtitle %>
        </p>
        <div class="mt-4 inline-flex items-center px-4 py-2 rounded-full bg-white/10 text-sm text-gray-300">
            <svg class="w-4 h-4 mr-2 text-green-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M8 7V3m8 4V3m-9 8h10M5 21h14a2 2 0 002-2V7a2 2 0 00-2-2H5a2 2 0 00-2 2v12a2 2 0 002 2z"></path>
            </svg>
            <%= t.hero.lastUpdatedLabel %> <%= t.hero.lastUpdatedDate %>
        </div>
    </div>
</section>

<!-- Main Content Section -->
<section class="relative bg-surface-primary py-16 sm:py-20 overflow-hidden">
    <div class="absolute inset-0 grid-pattern opacity-20"></div>
    <div class="blur-circle blur-circle--indigo w-64 h-64 top-1/4 right-0 opacity-10"></div>
    <div class="blur-circle blur-circle--green w-64 h-64 bottom-1/4 left-0 opacity-10"></div>

    <div class="relative z-10 max-w-4xl mx-auto px-4 sm:px-6 lg:px-8">

        <!-- 1. Data Controller -->
        <div class="card card--glow mb-8 animate-on-scroll section-anchor" id="controller">
            <h2 class="text-2xl font-bold text-white mb-4"><%= t.controller.title %></h2>
            <p class="text-gray-300 leading-relaxed mb-3">
                <%= t.controller.p1 %>
            </p>
            <div class="p-4 rounded-lg bg-white/5 border border-white/10 text-gray-300 leading-relaxed">
                <%- t.controller.identityHtml %>
            </div>
            <p class="text-gray-400 text-sm mt-3">
                <%= t.controller.note %>
            </p>
        </div>

        <!-- 2. Purposes and Legal Bases -->
        <div class="card card--glow mb-8 animate-on-scroll section-anchor" id="purposes">
            <h2 class="text-2xl font-bold text-white mb-4"><%= t.purposes.title %></h2>
            <p class="text-gray-300 leading-relaxed mb-4">
                <%= t.purposes.intro %>
            </p>
            <div class="overflow-x-auto">
                <table class="policy-table">
                    <thead>
                        <tr>
                            <% t.purposes.headers.forEach(function (h) { %>
                            <th><%= h %></th>
                            <% }); %>
                        </tr>
                    </thead>
                    <tbody>
                        <% t.purposes.rows.forEach(function (r) { %>
                        <tr>
                            <td><%- r.purposeHtml %></td>
                            <td><%= r.data %></td>
                            <td><%- r.basisHtml %></td>
                        </tr>
                        <% }); %>
                    </tbody>
                </table>
            </div>
        </div>

        <!-- 3. Data Categories -->
        <div class="card card--glow mb-8 animate-on-scroll section-anchor" id="data-categories">
            <h2 class="text-2xl font-bold text-white mb-4"><%= t.dataCategories.title %></h2>
            <ul class="space-y-3 text-gray-300">
                <% t.dataCategories.items.forEach(function (it) { %>
                <li class="flex items-start gap-3">
                    <%- include('./includes/_checkmark-icon.ejs') %>
                    <span><%- it %></span>
                </li>
                <% }); %>
            </ul>
            <p class="text-gray-400 text-sm mt-4">
                <%= t.dataCategories.note %>
            </p>
        </div>

        <!-- 4. Recipients and Subprocessors -->
        <div class="card card--glow mb-8 animate-on-scroll section-anchor" id="subprocessors">
            <h2 class="text-2xl font-bold text-white mb-4"><%= t.subprocessors.title %></h2>
            <p class="text-gray-300 leading-relaxed mb-4">
                <%- t.subprocessors.introHtml %>
            </p>

            <% t.subprocessors.groups.forEach(function (g) { %>
            <h3 class="text-lg font-semibold text-white mt-6 mb-3"><%= g.title %></h3>
            <div class="overflow-x-auto">
                <table class="policy-table">
                    <thead>
                        <tr>
                            <% g.headers.forEach(function (h) { %>
                            <th><%= h %></th>
                            <% }); %>
                        </tr>
                    </thead>
                    <tbody>
                        <% g.rows.forEach(function (row) { %>
                        <tr>
                            <% row.forEach(function (cell) { %>
                            <td><%= cell %></td>
                            <% }); %>
                        </tr>
                        <% }); %>
                    </tbody>
                </table>
            </div>
            <% }); %>

            <p class="text-gray-400 text-sm mt-4">
                <%= t.subprocessors.note %>
            </p>
        </div>

        <!-- 5. Transfers Outside the EU -->
        <div class="card card--glow mb-8 animate-on-scroll section-anchor" id="transfers">
            <h2 class="text-2xl font-bold text-white mb-4"><%= t.transfers.title %></h2>
            <p class="text-gray-300 leading-relaxed mb-4">
                <%- t.transfers.p1Html %>
            </p>
            <div class="p-4 rounded-lg bg-amber-900/20 border border-amber-500/30 text-amber-200 text-sm leading-relaxed">
                <%- t.transfers.cloudActHtml %>
            </div>
        </div>

        <!-- 6. Retention Periods -->
        <div class="card card--glow mb-8 animate-on-scroll section-anchor" id="retention">
            <h2 class="text-2xl font-bold text-white mb-4"><%= t.retention.title %></h2>
            <div class="overflow-x-auto">
                <table class="policy-table">
                    <thead>
                        <tr>
                            <% t.retention.headers.forEach(function (h) { %>
                            <th><%= h %></th>
                            <% }); %>
                        </tr>
                    </thead>
                    <tbody>
                        <% t.retention.rows.forEach(function (row) { %>
                        <tr>
                            <% row.forEach(function (cell) { %>
                            <td><%= cell %></td>
                            <% }); %>
                        </tr>
                        <% }); %>
                    </tbody>
                </table>
            </div>
        </div>

        <!-- 7. Your Rights -->
        <div class="card card--glow mb-8 animate-on-scroll section-anchor" id="rights">
            <h2 class="text-2xl font-bold text-white mb-4"><%= t.rights.title %></h2>
            <p class="text-gray-300 leading-relaxed mb-4">
                <%= t.rights.intro %>
            </p>
            <ul class="space-y-3 text-gray-300 mb-4">
                <% t.rights.items.forEach(function (it) { %>
                <li class="flex items-start gap-3">
                    <%- include('./includes/_checkmark-icon.ejs') %>
                    <span><%- it %></span>
                </li>
                <% }); %>
            </ul>
            <div class="p-4 rounded-lg bg-white/5 border border-white/10 text-gray-300 text-sm leading-relaxed">
                <%- t.rights.contactHtml %>
            </div>
        </div>

        <!-- 8. Right to Lodge a Complaint -->
        <div class="card card--glow mb-8 animate-on-scroll section-anchor" id="cnil">
            <h2 class="text-2xl font-bold text-white mb-4"><%= t.cnil.title %></h2>
            <p class="text-gray-300 leading-relaxed mb-4">
                <%= t.cnil.p1 %>
            </p>
            <div class="p-4 rounded-lg bg-white/5 border border-white/10 text-gray-300 leading-relaxed">
                <%- t.cnil.addressHtml %>
            </div>
            <p class="text-gray-400 text-sm mt-3">
                <%= t.cnil.note %>
            </p>
        </div>

        <!-- 9. Cookies and Trackers -->
        <div class="card card--glow mb-8 animate-on-scroll section-anchor" id="cookies">
            <h2 class="text-2xl font-bold text-white mb-4"><%= t.cookies.title %></h2>
            <p class="text-gray-300 leading-relaxed mb-4">
                <%= t.cookies.intro %>
            </p>
            <ul class="space-y-2 text-gray-300 mb-4">
                <% t.cookies.items.forEach(function (it) { %>
                <li class="flex items-start gap-3">
                    <%- include('./includes/_checkmark-icon.ejs') %>
                    <span><%- it %></span>
                </li>
                <% }); %>
            </ul>
            <p class="text-gray-300 leading-relaxed mb-4">
                <%- t.cookies.consentScopeHtml %>
            </p>
            <div class="flex flex-wrap gap-4 mb-4">
                <button type="button" onclick="window.hook0Consent && window.hook0Consent.reset()" class="btn btn--secondary btn--sm">
                    <%= t.cookies.changeButton %>
                </button>
            </div>
            <% if (process.env.MATOMO_URL && process.env.MATOMO_SITE_ID) { %>
            <div id="matomo-opt-out" class="p-4 rounded-lg bg-surface-secondary mt-4"></div>
            <script src="<%= process.env.MATOMO_URL %>/index.php?module=CoreAdminHome&action=optOutJS&divId=matomo-opt-out&language=auto&showIntro=1"></script>
            <% } %>
        </div>

        <!-- 9b. Server-side Conversion Measurement -->
        <div class="card card--glow mb-8 animate-on-scroll section-anchor" id="server-side-tracking">
            <h2 class="text-2xl font-bold text-white mb-4"><%= t.serverSideTracking.title %></h2>
            <p class="text-gray-300 leading-relaxed mb-4">
                <%= t.serverSideTracking.intro %>
            </p>
            <ul class="space-y-2 text-gray-300 mb-4">
                <% t.serverSideTracking.items.forEach(function (it) { %>
                <li class="flex items-start gap-3">
                    <%- include('./includes/_checkmark-icon.ejs') %>
                    <span><%- it %></span>
                </li>
                <% }); %>
            </ul>
            <p class="text-gray-400 text-sm">
                <%- t.serverSideTracking.footnoteHtml %>
            </p>
        </div>

        <!-- 10. Security -->
        <div class="card card--glow mb-8 animate-on-scroll section-anchor" id="security">
            <h2 class="text-2xl font-bold text-white mb-4"><%= t.security.title %></h2>
            <p class="text-gray-300 leading-relaxed mb-3">
                <%= t.security.p1 %>
            </p>
            <p class="text-gray-300 leading-relaxed mb-3">
                <%- t.security.p2Html %>
            </p>
            <p class="text-gray-300 leading-relaxed">
                <%- t.security.p3Html %>
            </p>
        </div>

        <!-- 11. Changes -->
        <div class="card card--glow mb-8 animate-on-scroll section-anchor" id="changes">
            <h2 class="text-2xl font-bold text-white mb-4"><%= t.changes.title %></h2>
            <p class="text-gray-300 leading-relaxed">
                <%= t.changes.p1 %>
            </p>
        </div>

    </div>
</section>

<%- include('./includes/_footer.ejs') %>

</body>
</html>
