<!DOCTYPE html>
<html lang="en">
<head>
    <title>Webhook Platform — Send, Sign, Retry & Monitor Webhooks | Hook0</title>
    <%
    locals.pageTitle = 'Webhook Platform — Send, Sign, Retry & Monitor Webhooks | Hook0';
    locals.pageDescription = 'Hook0 is an open-source webhook platform that delivers events with HMAC signatures, configurable retries, dead letter queues and a built-in subscriber portal. Free forever, no credit card.';
    locals.pageCanonical = '/webhook-platform';
    locals.pageFAQSchema = {
      "@context": "https://schema.org",
      "@type": "FAQPage",
      "mainEntity": [
        {
          "@type": "Question",
          "name": "What is a webhook platform?",
          "acceptedAnswer": {
            "@type": "Answer",
            "text": "A webhook platform is the production infrastructure that sits between your application and your customers' endpoints. It signs payloads with HMAC, retries on failures with configurable backoff, stores delivery logs, and exposes a subscriber portal so your users can manage their own endpoints. Hook0 ships all of this as a managed service or a self-hosted Docker stack."
          }
        },
        {
          "@type": "Question",
          "name": "Why use a webhook platform instead of building your own?",
          "acceptedAnswer": {
            "@type": "Answer",
            "text": "Building a production-grade webhook system means 3+ engineering sprints for retries, dead letter queues, HMAC signatures, monitoring, and a subscriber UI — plus ongoing maintenance forever. A webhook platform like Hook0 lets you ship in 30 minutes and reclaim that engineering time for product work."
          }
        },
        {
          "@type": "Question",
          "name": "Is Hook0's webhook platform free?",
          "acceptedAnswer": {
            "@type": "Answer",
            "text": "Yes. Hook0 has a free forever tier with no credit card required. You also get the option to self-host the same codebase on your own infrastructure for free under SSPL-1.0. Paid plans only unlock higher volume and dedicated support — no feature gating."
          }
        },
        {
          "@type": "Question",
          "name": "Can I self-host the Hook0 webhook platform?",
          "acceptedAnswer": {
            "@type": "Answer",
            "text": "Yes. The entire Hook0 webhook platform is open-source under SSPL-1.0 and ships with Docker Compose and Kubernetes manifests. Self-hosting is free, with no enterprise tier required, and the self-hosted edition has the same features as the cloud."
          }
        },
        {
          "@type": "Question",
          "name": "How does Hook0 compare to Svix, Hookdeck or Convoy?",
          "acceptedAnswer": {
            "@type": "Answer",
            "text": "Hook0 is fully open-source (not open-core), 100% bootstrapped (no VC pressure on pricing), and self-hostable on any plan. Svix is open-core with an enterprise tier; Hookdeck is closed-source and US-only; Convoy is open-source but community-maintained. See the side-by-side comparisons for the details."
          }
        }
      ]
    };
    %>
    <%- include('./includes/_head.ejs') %>
</head>
<body>
<%- include('./includes/_header.ejs') %>

<!-- Hero Section -->
<section class="relative gradient-hero overflow-hidden py-20 sm:py-28">
    <div class="absolute inset-0 grid-pattern opacity-50"></div>
    <div class="blur-circle blur-circle--indigo w-96 h-96 -top-48 -left-48"></div>
    <div class="blur-circle blur-circle--green w-80 h-80 top-1/2 -right-40"></div>

    <div class="relative z-10 max-w-4xl mx-auto px-4 sm:px-6 lg:px-8 text-center">
        <span class="section__eyebrow">Webhook Platform</span>
        <h1 class="mt-4 text-display-xl text-white">
            The Open-Source Webhook Platform
            <span class="block text-gradient-green">Built for Production</span>
        </h1>
        <p class="mt-6 text-body-lg text-gray-400 max-w-2xl mx-auto">
            Hook0 handles the parts you keep putting off. HMAC-SHA256 signing on every payload. Retries that actually back off properly. A DLQ when a customer's endpoint has been 502'ing for six hours. And a subscriber portal so your users stop opening tickets to rotate a secret. Self-host it or use the cloud. No vendor lock-in either way.
        </p>

        <div class="mt-10 flex flex-col sm:flex-row gap-4 justify-center">
            <a href="https://app.hook0.com/register" class="btn btn--primary btn--lg group" onclick="trackEvent('CTA', 'Click', 'webhook-platform-hero-register')">
                Start Free
                <svg class="w-5 h-5 ml-2 group-hover:translate-x-1 transition-transform" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 7l5 5m0 0l-5 5m5-5H6"></path>
                </svg>
            </a>
            <a href="https://play.hook0.com" target="_blank" rel="noopener" class="btn btn--secondary btn--lg" onclick="trackEvent('CTA', 'Click', 'webhook-platform-hero-playground')">
                Try the Playground
            </a>
        </div>
        <p class="mt-4 text-sm text-gray-500">100 events/day free. No credit card. Open-source.</p>
    </div>
</section>

<%- include('./includes/_social-proof-bar.ejs') %>

<!-- Capabilities -->
<section class="section relative overflow-hidden">
    <div class="absolute inset-0 grid-pattern opacity-20"></div>
    <div class="blur-circle blur-circle--green w-64 h-64 top-1/4 right-0 opacity-10"></div>

    <div class="relative z-10 max-w-5xl mx-auto px-4 sm:px-6 lg:px-8">
        <div class="text-center mb-12 animate-on-scroll">
            <span class="section__eyebrow">Capabilities</span>
            <h2 class="mt-4 text-display font-bold text-white">A webhook platform you don't have to write yourself</h2>
            <p class="mt-4 text-body text-gray-400 max-w-2xl mx-auto">A production webhook system is roughly 3 sprints of plumbing. Skip that.</p>
        </div>

        <div class="grid md:grid-cols-2 gap-8 stagger-children">
            <div class="animate-on-scroll card card--glow p-8">
                <h3 class="text-xl font-bold text-white mb-3">HMAC signing</h3>
                <p class="text-gray-400">Payloads are signed so subscribers can verify the request came from you. Timestamps cover replay attacks.</p>
            </div>
            <div class="animate-on-scroll card card--glow p-8">
                <h3 class="text-xl font-bold text-white mb-3">Two-phase retries</h3>
                <p class="text-gray-400">Fast retries for flaky endpoints, slow retries over days for real outages. Tunable per subscription, with a DLQ at the end.</p>
            </div>
            <div class="animate-on-scroll card card--glow p-8">
                <h3 class="text-xl font-bold text-white mb-3">Delivery logs</h3>
                <p class="text-gray-400">Request, response, status code, latency. Replay any event from the dashboard. Support stops guessing.</p>
            </div>
            <div class="animate-on-scroll card card--glow p-8">
                <h3 class="text-xl font-bold text-white mb-3">Subscriber portal</h3>
                <p class="text-gray-400">A drop-in UI where your users manage their endpoints, secrets and event filters. No more support tickets to rotate a key.</p>
            </div>
            <div class="animate-on-scroll card card--glow p-8">
                <h3 class="text-xl font-bold text-white mb-3">SSPL, no open-core</h3>
                <p class="text-gray-400">Same code in cloud and self-hosted. Docker Compose or Kubernetes. No enterprise tier hiding the useful features.</p>
            </div>
            <div class="animate-on-scroll card card--glow p-8">
                <h3 class="text-xl font-bold text-white mb-3">Hosted in Europe</h3>
                <p class="text-gray-400">EU data residency, GDPR by default. Matters when your customers care where their events land.</p>
            </div>
        </div>
    </div>
</section>

<!-- How it works -->
<section class="section relative overflow-hidden">
    <div class="absolute inset-0 grid-pattern opacity-20"></div>
    <div class="relative z-10 max-w-4xl mx-auto px-4 sm:px-6 lg:px-8">
        <div class="text-center mb-12 animate-on-scroll">
            <span class="section__eyebrow">Code</span>
            <h2 class="mt-4 text-display font-bold text-white">One API call to deliver an event</h2>
        </div>

        <div class="animate-on-scroll card card--glow p-6 overflow-x-auto">
<pre class="text-sm text-gray-300"><code>// Trigger an event from anywhere in your backend.
await hook0.message.create("&lt;application_id&gt;", {
  event_type: "invoice.paid",
  event_id:   "evt_Wqb1k73rXprtTm7Qdlr38G",
  payload: {
    invoice_id: "in_8X9aBcDeFgHiJk",
    status:     "paid",
    amount_eur: 4990
  }
});

// What Hook0 does after this call:
// signs the payload with HMAC, fans out to every matching
// subscriber, retries failed deliveries on a two-phase schedule,
// and stores the request/response for replay.
</code></pre>
        </div>

        <div class="mt-8 text-center">
            <a href="https://documentation.hook0.com/docs/getting-started" class="text-indigo-400 hover:text-indigo-300 underline" onclick="trackEvent('CTA', 'Click', 'webhook-platform-docs')">Read the getting-started guide →</a>
        </div>
    </div>
</section>

<!-- Build vs Buy -->
<section class="section relative overflow-hidden">
    <div class="relative z-10 max-w-5xl mx-auto px-4 sm:px-6 lg:px-8">
        <div class="text-center mb-12 animate-on-scroll">
            <span class="section__eyebrow">Build vs Buy</span>
            <h2 class="mt-4 text-display font-bold text-white">What rolling your own actually costs</h2>
        </div>

        <div class="animate-on-scroll overflow-x-auto">
            <table class="w-full text-left border-collapse">
                <thead>
                    <tr class="border-b border-white/10">
                        <th class="py-4 px-6 text-sm font-semibold text-gray-400 uppercase tracking-wider">Concern</th>
                        <th class="py-4 px-6 text-sm font-semibold text-gray-400 uppercase tracking-wider">Build it yourself</th>
                        <th class="py-4 px-6 text-sm font-semibold text-green-400 uppercase tracking-wider">Hook0 webhook platform</th>
                    </tr>
                </thead>
                <tbody class="divide-y divide-white/5">
                    <tr><td class="py-4 px-6 text-gray-300">Time to first webhook</td><td class="py-4 px-6 text-gray-400">3+ sprints</td><td class="py-4 px-6 text-green-400 font-medium">30 minutes</td></tr>
                    <tr><td class="py-4 px-6 text-gray-300">Retry logic</td><td class="py-4 px-6 text-gray-400">You design backoff and DLQ</td><td class="py-4 px-6 text-green-400 font-medium">Two-phase, configurable</td></tr>
                    <tr><td class="py-4 px-6 text-gray-300">HMAC signing and verification</td><td class="py-4 px-6 text-gray-400">You implement and document</td><td class="py-4 px-6 text-green-400 font-medium">Included, documented</td></tr>
                    <tr><td class="py-4 px-6 text-gray-300">Subscriber UI</td><td class="py-4 px-6 text-gray-400">Another sprint</td><td class="py-4 px-6 text-green-400 font-medium">Drop-in portal</td></tr>
                    <tr><td class="py-4 px-6 text-gray-300">Replay and debugging</td><td class="py-4 px-6 text-gray-400">Custom dashboards</td><td class="py-4 px-6 text-green-400 font-medium">Built-in logs and replay</td></tr>
                    <tr><td class="py-4 px-6 text-gray-300">Ongoing maintenance</td><td class="py-4 px-6 text-gray-400">Forever, your team</td><td class="py-4 px-6 text-green-400 font-medium">Ours</td></tr>
                    <tr><td class="py-4 px-6 text-gray-300">Lock-in risk</td><td class="py-4 px-6 text-gray-400">N/A (your code)</td><td class="py-4 px-6 text-green-400 font-medium">None. Full source, self-hostable.</td></tr>
                </tbody>
            </table>
        </div>

        <div class="mt-8 text-center">
            <a href="./build-vs-buy-webhooks" class="text-indigo-400 hover:text-indigo-300 underline">Read the full build vs buy breakdown →</a>
        </div>
    </div>
</section>

<!-- FAQ Section -->
<section class="section relative overflow-hidden">
    <div class="absolute inset-0 grid-pattern opacity-20"></div>
    <div class="relative z-10 max-w-3xl mx-auto px-4 sm:px-6 lg:px-8">
        <div class="text-center mb-12 animate-on-scroll">
            <span class="section__eyebrow">FAQ</span>
            <h2 class="mt-4 text-display font-bold text-white">Webhook platform questions</h2>
        </div>

        <div class="space-y-6 stagger-children">
            <div class="animate-on-scroll card card--glow p-6">
                <h3 class="text-lg font-semibold text-white mb-2">What is a webhook platform?</h3>
                <p class="text-gray-400">A webhook platform is the production infrastructure that sits between your application and your customers' endpoints. It signs payloads with HMAC, retries on failures with configurable backoff, stores delivery logs, and exposes a subscriber portal so your users can manage their own endpoints. Hook0 ships all of this as a managed service or a self-hosted Docker stack.</p>
            </div>
            <div class="animate-on-scroll card card--glow p-6">
                <h3 class="text-lg font-semibold text-white mb-2">Why use a webhook platform instead of building your own?</h3>
                <p class="text-gray-400">Building a production-grade webhook system means 3+ engineering sprints for retries, dead letter queues, HMAC signatures, monitoring, and a subscriber UI — plus ongoing maintenance forever. A webhook platform like Hook0 lets you ship in 30 minutes and reclaim that engineering time for product work.</p>
            </div>
            <div class="animate-on-scroll card card--glow p-6">
                <h3 class="text-lg font-semibold text-white mb-2">Is Hook0's webhook platform free?</h3>
                <p class="text-gray-400">Yes. Hook0 has a free forever tier with no credit card required. You also get the option to self-host the same codebase on your own infrastructure for free under SSPL-1.0. Paid plans only unlock higher volume and dedicated support — no feature gating.</p>
            </div>
            <div class="animate-on-scroll card card--glow p-6">
                <h3 class="text-lg font-semibold text-white mb-2">Can I self-host the Hook0 webhook platform?</h3>
                <p class="text-gray-400">Yes. The entire Hook0 webhook platform is open-source under SSPL-1.0 and ships with Docker Compose and Kubernetes manifests. Self-hosting is free, with no enterprise tier required, and the self-hosted edition has the same features as the cloud.</p>
            </div>
            <div class="animate-on-scroll card card--glow p-6">
                <h3 class="text-lg font-semibold text-white mb-2">How does Hook0 compare to Svix, Hookdeck or Convoy?</h3>
                <p class="text-gray-400">Hook0 is fully open-source (not open-core), 100% bootstrapped (no VC pressure on pricing), and self-hostable on any plan. Svix is open-core with an enterprise tier; Hookdeck is closed-source and US-only; Convoy is open-source but community-maintained. See the side-by-side comparisons for the details.</p>
            </div>
        </div>
    </div>
</section>

<!-- Related Pages -->
<section class="py-12 relative">
    <div class="max-w-4xl mx-auto px-4 sm:px-6 lg:px-8">
        <h2 class="text-lg font-semibold text-gray-400 mb-6 text-center">Related</h2>
        <div class="flex flex-wrap justify-center gap-4 text-sm">
            <a href="./webhook-free" class="text-indigo-400 hover:text-indigo-300 underline">Free Webhooks</a>
            <a href="./webhook-api" class="text-indigo-400 hover:text-indigo-300 underline">Webhook API</a>
            <a href="./hook0-vs-svix" class="text-indigo-400 hover:text-indigo-300 underline">Hook0 vs Svix</a>
            <a href="./hook0-vs-hookdeck" class="text-indigo-400 hover:text-indigo-300 underline">Hook0 vs Hookdeck</a>
            <a href="./hook0-vs-convoy" class="text-indigo-400 hover:text-indigo-300 underline">Hook0 vs Convoy</a>
            <a href="./build-vs-buy-webhooks" class="text-indigo-400 hover:text-indigo-300 underline">Build vs Buy Webhooks</a>
            <a href="./self-hosted-webhooks" class="text-indigo-400 hover:text-indigo-300 underline">Self-Hosted Webhooks</a>
            <a href="./open-source-webhooks" class="text-indigo-400 hover:text-indigo-300 underline">Open-Source Webhooks</a>
        </div>
    </div>
</section>

<!-- CTA Section -->
<%- include('./includes/_cta-inline.ejs') %>

<%- include('./includes/_footer.ejs') %>

</body>
</html>
