// Generated by hook0-sdkgen from the OpenAPI snapshot the API crate commits.
// Do not edit by hand: run `UPDATE_SDK=kotlin cargo test -p hook0-sdkgen sdk_targets` and commit the result.

package com.hook0.kotlin.generated

import com.hook0.kotlin.QueryParameter
import com.hook0.kotlin.Transport
import com.hook0.kotlin.Wire

/**
 * What the API declares under `events`, issued through the transport it is handed.
 *
 * Every call suspends until the API has answered, and holds no thread while it waits.
 *
 * @param transport what one request is issued through
 */
class EventsSuspendingApi(private val transport: Transport) {

  /**
   * Get an event by its ID
   *
   * @param eventId carries `event_id`.
   * @param applicationId carries `application_id`.
   * @return what the API answered, once it has
   */
  suspend fun get(eventId: String, applicationId: String): EventWithPayload {
    var path = "/api/v1/events/{event_id}"
    path = path.replace("{event_id}", Wire.pathSegment(eventId))
    val query = ArrayList<QueryParameter>()
    query.add(QueryParameter("application_id", Wire.queryValue(applicationId)))
    return Problems.readAnswer(
      transport.requestSuspending("GET", path, query, null),
      EventWithPayload.Companion::fromJson
    )
  }

  /**
   * Ingest an event
   *
   * @param body the EventPost the operation reads
   * @return what the API answered, once it has
   */
  suspend fun ingest(body: EventPost): IngestedEvent {
    val path = "/api/v1/event/"
    val query = ArrayList<QueryParameter>()
    return Problems.readAnswer(
      transport.requestSuspending("POST", path, query, body.toJson()),
      IngestedEvent.Companion::fromJson
    )
  }

  /**
   * List latest events
   *
   * @param applicationId carries `application_id`.
   * @return what the API answered, once it has
   */
  suspend fun list(applicationId: String): List<Event> {
    val path = "/api/v1/events/"
    val query = ArrayList<QueryParameter>()
    query.add(QueryParameter("application_id", Wire.queryValue(applicationId)))
    return Problems.readAnswer(
      transport.requestSuspending("GET", path, query, null),
      Wire.asList(Event.Companion::fromJson)
    )
  }

  /**
   * Replay an event
   *
   * @param eventId carries `event_id`.
   * @param body the ReplayEvent the operation reads
   */
  suspend fun replay(eventId: String, body: ReplayEvent) {
    var path = "/api/v1/events/{event_id}/replay"
    path = path.replace("{event_id}", Wire.pathSegment(eventId))
    val query = ArrayList<QueryParameter>()
    Problems.checkAnswer(transport.requestSuspending("POST", path, query, body.toJson()))
  }
}
