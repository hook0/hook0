// Generated by hook0-sdkgen from the OpenAPI snapshot the API crate commits.
// Do not edit by hand: run `UPDATE_SDK=kotlin cargo test -p hook0-sdkgen sdk_targets` and commit the result.

package com.hook0.kotlin.generated

import com.hook0.kotlin.QueryParameter
import com.hook0.kotlin.Transport
import com.hook0.kotlin.Wire

/**
 * What the API declares under `events_per_day`, issued through the transport it is handed.
 *
 * Every call blocks until the API has answered.
 *
 * @param transport what one request is issued through
 */
class EventsPerDayApi(private val transport: Transport) {

  /**
   * List events per day for an application
   *
   * @param applicationId carries `application_id`.
   * @param from carries `from`, or nothing when the caller sends none: Start of date range (inclusive). Defaults to 30
   *     days before `to`.
   * @param to carries `to`, or nothing when the caller sends none: End of date range (inclusive). Defaults to today.
   * @return what the API answered
   */
  fun listForApplication(applicationId: String, from: String? = null, to: String? = null): List<EventsPerDayEntry> {
    val path = "/api/v1/events_per_day/application"
    val query = ArrayList<QueryParameter>()
    query.add(QueryParameter("application_id", Wire.queryValue(applicationId)))
    if (from != null) {
      query.add(QueryParameter("from", Wire.queryValue(from)))
    }
    if (to != null) {
      query.add(QueryParameter("to", Wire.queryValue(to)))
    }
    return Problems.readAnswer(
      transport.request("GET", path, query, null),
      Wire.asList(EventsPerDayEntry.Companion::fromJson)
    )
  }

  /**
   * List events per day for an organization
   *
   * @param organizationId carries `organization_id`.
   * @param from carries `from`, or nothing when the caller sends none: Start of date range (inclusive). Defaults to 30
   *     days before `to`.
   * @param to carries `to`, or nothing when the caller sends none: End of date range (inclusive). Defaults to today.
   * @return what the API answered
   */
  fun listForOrganization(organizationId: String, from: String? = null, to: String? = null): List<EventsPerDayEntry> {
    val path = "/api/v1/events_per_day/organization"
    val query = ArrayList<QueryParameter>()
    query.add(QueryParameter("organization_id", Wire.queryValue(organizationId)))
    if (from != null) {
      query.add(QueryParameter("from", Wire.queryValue(from)))
    }
    if (to != null) {
      query.add(QueryParameter("to", Wire.queryValue(to)))
    }
    return Problems.readAnswer(
      transport.request("GET", path, query, null),
      Wire.asList(EventsPerDayEntry.Companion::fromJson)
    )
  }
}
