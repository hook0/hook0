// Generated by hook0-sdkgen from the OpenAPI snapshot the API crate commits.
// Do not edit by hand: run `UPDATE_SDK=java cargo test -p hook0-sdkgen sdk_targets` and commit the result.

package com.hook0.client.generated;

import com.hook0.client.Wire;
import java.util.LinkedHashMap;
import java.util.Map;

/**
 * The `InstanceConfig` the API declares.
 *
 * @param applicationSecretCompatibility carries `application_secret_compatibility`.
 * @param autoDbMigration carries `auto_db_migration`.
 * @param biscuitPublicKey carries `biscuit_public_key`.
 * @param passwordMinimumLength carries `password_minimum_length`.
 * @param quotaEnforcement carries `quota_enforcement`.
 * @param registrationDisabled carries `registration_disabled`.
 * @param supportEmailAddress carries `support_email_address`.
 * @param cloudflareTurnstileSiteKey carries `cloudflare_turnstile_site_key`, or nothing when the API answers none.
 * @param formbricks carries `formbricks`, or nothing when the API answers none.
 * @param matomo carries `matomo`, or nothing when the API answers none.
 */
public record InstanceConfig(
    Boolean applicationSecretCompatibility,
    Boolean autoDbMigration,
    String biscuitPublicKey,
    Integer passwordMinimumLength,
    Boolean quotaEnforcement,
    Boolean registrationDisabled,
    String supportEmailAddress,
    String cloudflareTurnstileSiteKey,
    InstanceConfigFormbricks formbricks,
    InstanceConfigMatomo matomo) {

  /**
   * Reads one out of what the API answered.
   *
   * @param value the JSON document the API answered
   * @return the InstanceConfig the API declares
   */
  public static InstanceConfig fromJson(Object value) {
    Map<String, Object> fields = Wire.asFields(value, "InstanceConfig");
    return new InstanceConfig(
        Wire.read(fields, "application_secret_compatibility", Wire::asBoolean),
        Wire.read(fields, "auto_db_migration", Wire::asBoolean),
        Wire.read(fields, "biscuit_public_key", Wire::asText),
        Wire.read(fields, "password_minimum_length", Wire::asInteger),
        Wire.read(fields, "quota_enforcement", Wire::asBoolean),
        Wire.read(fields, "registration_disabled", Wire::asBoolean),
        Wire.read(fields, "support_email_address", Wire::asText),
        Wire.maybe(fields, "cloudflare_turnstile_site_key", Wire::asText),
        Wire.maybe(fields, "formbricks", InstanceConfigFormbricks::fromJson),
        Wire.maybe(fields, "matomo", InstanceConfigMatomo::fromJson));
  }

  /**
   * Writes one back the way the API reads it.
   *
   * @return the document the API reads
   */
  public Map<String, Object> toJson() {
    Map<String, Object> out = new LinkedHashMap<>();
    out.put("application_secret_compatibility", applicationSecretCompatibility);
    out.put("auto_db_migration", autoDbMigration);
    out.put("biscuit_public_key", biscuitPublicKey);
    out.put("password_minimum_length", passwordMinimumLength);
    out.put("quota_enforcement", quotaEnforcement);
    out.put("registration_disabled", registrationDisabled);
    out.put("support_email_address", supportEmailAddress);
    if (cloudflareTurnstileSiteKey != null) {
      out.put("cloudflare_turnstile_site_key", cloudflareTurnstileSiteKey);
    }
    if (formbricks != null) {
      out.put("formbricks", formbricks.toJson());
    }
    if (matomo != null) {
      out.put("matomo", matomo.toJson());
    }
    return out;
  }
}
