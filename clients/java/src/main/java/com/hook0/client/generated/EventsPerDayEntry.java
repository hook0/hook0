// Generated by hook0-sdkgen from the OpenAPI snapshot the API crate commits.
// Do not edit by hand: run `UPDATE_SDK=java cargo test -p hook0-sdkgen sdk_targets` and commit the result.

package com.hook0.client.generated;

import com.hook0.client.Wire;
import java.time.LocalDate;
import java.util.LinkedHashMap;
import java.util.Map;
import java.util.UUID;

/**
 * The `EventsPerDayEntry` the API declares.
 *
 * @param amount carries `amount`.
 * @param applicationId carries `application_id`.
 * @param applicationName carries `application_name`.
 * @param date carries `date`.
 * @param isProvisional carries `is_provisional`.
 */
public record EventsPerDayEntry(
    Integer amount,
    UUID applicationId,
    String applicationName,
    LocalDate date,
    Boolean isProvisional) {

  /**
   * Reads one out of what the API answered.
   *
   * @param value the JSON document the API answered
   * @return the EventsPerDayEntry the API declares
   */
  public static EventsPerDayEntry fromJson(Object value) {
    Map<String, Object> fields = Wire.asFields(value, "EventsPerDayEntry");
    return new EventsPerDayEntry(
        Wire.read(fields, "amount", Wire::asInteger),
        Wire.read(fields, "application_id", Wire::asUuid),
        Wire.read(fields, "application_name", Wire::asText),
        Wire.read(fields, "date", Wire::asDay),
        Wire.read(fields, "is_provisional", Wire::asBoolean));
  }

  /**
   * Writes one back the way the API reads it.
   *
   * @return the document the API reads
   */
  public Map<String, Object> toJson() {
    Map<String, Object> out = new LinkedHashMap<>();
    out.put("amount", amount);
    out.put("application_id", Wire.writeUuid(applicationId));
    out.put("application_name", applicationName);
    out.put("date", Wire.writeDay(date));
    out.put("is_provisional", isProvisional);
    return out;
  }
}
