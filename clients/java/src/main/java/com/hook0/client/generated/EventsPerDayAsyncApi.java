// Generated by hook0-sdkgen from the OpenAPI snapshot the API crate commits.
// Do not edit by hand: run `UPDATE_SDK=java cargo test -p hook0-sdkgen sdk_targets` and commit the result.

package com.hook0.client.generated;

import com.hook0.client.QueryParameter;
import com.hook0.client.Transport;
import com.hook0.client.Wire;
import java.util.ArrayList;
import java.util.List;
import java.util.concurrent.CompletableFuture;

/**
 * What the API declares under `events_per_day`, issued through the transport it is handed.
 *
 * Every call hands back what the API will answer.
 */
public final class EventsPerDayAsyncApi {

  private final Transport transport;

  /**
   * Builds the group on what its requests are issued through.
   *
   * @param transport what one request is issued through
   */
  public EventsPerDayAsyncApi(Transport transport) {
    this.transport = transport;
  }

  /**
   * List events per day for an application
   *
   * @param applicationId carries `application_id`.
   * @param from carries `from`, or nothing when the caller sends none: Start of date range (inclusive). Defaults to 30
   *     days before `to`.
   * @param to carries `to`, or nothing when the caller sends none: End of date range (inclusive). Defaults to today.
   * @return what the API will answer
   */
  public CompletableFuture<List<EventsPerDayEntry>> listForApplication(String applicationId, String from, String to) {
    String path = "/api/v1/events_per_day/application";
    List<QueryParameter> query = new ArrayList<>();
    query.add(new QueryParameter("application_id", Wire.queryValue(applicationId)));
    if (from != null) {
      query.add(new QueryParameter("from", Wire.queryValue(from)));
    }
    if (to != null) {
      query.add(new QueryParameter("to", Wire.queryValue(to)));
    }
    return transport.requestAsync("GET", path, query, null)
        .thenApply(Problems.readingWith(Wire.asList(EventsPerDayEntry::fromJson)));
  }

  /**
   * List events per day for an organization
   *
   * @param organizationId carries `organization_id`.
   * @param from carries `from`, or nothing when the caller sends none: Start of date range (inclusive). Defaults to 30
   *     days before `to`.
   * @param to carries `to`, or nothing when the caller sends none: End of date range (inclusive). Defaults to today.
   * @return what the API will answer
   */
  public CompletableFuture<List<EventsPerDayEntry>> listForOrganization(String organizationId, String from, String to) {
    String path = "/api/v1/events_per_day/organization";
    List<QueryParameter> query = new ArrayList<>();
    query.add(new QueryParameter("organization_id", Wire.queryValue(organizationId)));
    if (from != null) {
      query.add(new QueryParameter("from", Wire.queryValue(from)));
    }
    if (to != null) {
      query.add(new QueryParameter("to", Wire.queryValue(to)));
    }
    return transport.requestAsync("GET", path, query, null)
        .thenApply(Problems.readingWith(Wire.asList(EventsPerDayEntry::fromJson)));
  }
}
