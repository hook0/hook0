// Generated by hook0-sdkgen from the OpenAPI snapshot the API crate commits.
// Do not edit by hand: run `UPDATE_SDK=java cargo test -p hook0-sdkgen sdk_targets` and commit the result.

package com.hook0.client.generated;

import com.hook0.client.QueryParameter;
import com.hook0.client.Transport;
import com.hook0.client.Wire;
import java.util.ArrayList;
import java.util.List;

/**
 * What the API declares under `events`, issued through the transport it is handed.
 *
 * Every call blocks until the API has answered.
 */
public final class EventsApi {

  private final Transport transport;

  /**
   * Builds the group on what its requests are issued through.
   *
   * @param transport what one request is issued through
   */
  public EventsApi(Transport transport) {
    this.transport = transport;
  }

  /**
   * Get an event by its ID
   *
   * @param eventId carries `event_id`.
   * @param applicationId carries `application_id`.
   * @return what the API answered
   */
  public EventWithPayload get(String eventId, String applicationId) {
    String path = "/api/v1/events/{event_id}";
    path = path.replace("{event_id}", Wire.pathSegment(eventId));
    List<QueryParameter> query = new ArrayList<>();
    query.add(new QueryParameter("application_id", Wire.queryValue(applicationId)));
    return Problems.readAnswer(transport.request("GET", path, query, null), EventWithPayload::fromJson);
  }

  /**
   * Ingest an event
   *
   * @param body the EventPost the operation reads
   * @return what the API answered
   */
  public IngestedEvent ingest(EventPost body) {
    String path = "/api/v1/event/";
    List<QueryParameter> query = new ArrayList<>();
    return Problems.readAnswer(transport.request("POST", path, query, body.toJson()), IngestedEvent::fromJson);
  }

  /**
   * List latest events
   *
   * @param applicationId carries `application_id`.
   * @return what the API answered
   */
  public List<Event> list(String applicationId) {
    String path = "/api/v1/events/";
    List<QueryParameter> query = new ArrayList<>();
    query.add(new QueryParameter("application_id", Wire.queryValue(applicationId)));
    return Problems.readAnswer(transport.request("GET", path, query, null), Wire.asList(Event::fromJson));
  }

  /**
   * Replay an event
   *
   * @param eventId carries `event_id`.
   * @param body the ReplayEvent the operation reads
   */
  public void replay(String eventId, ReplayEvent body) {
    String path = "/api/v1/events/{event_id}/replay";
    path = path.replace("{event_id}", Wire.pathSegment(eventId));
    List<QueryParameter> query = new ArrayList<>();
    Problems.checkAnswer(transport.request("POST", path, query, body.toJson()));
  }
}
