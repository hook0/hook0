// Generated by hook0-sdkgen from the OpenAPI snapshot the API crate commits.
// Do not edit by hand: run `UPDATE_SDK=java cargo test -p hook0-sdkgen sdk_targets` and commit the result.

package com.hook0.client.generated;

import com.hook0.client.QueryParameter;
import com.hook0.client.Transport;
import com.hook0.client.Wire;
import java.util.ArrayList;
import java.util.List;

/**
 * What the API declares under `serviceToken`, issued through the transport it is handed.
 *
 * Every call blocks until the API has answered.
 */
public final class ServiceTokenApi {

  private final Transport transport;

  /**
   * Builds the group on what its requests are issued through.
   *
   * @param transport what one request is issued through
   */
  public ServiceTokenApi(Transport transport) {
    this.transport = transport;
  }

  /**
   * Create a new service token
   *
   * @param body the ServiceTokenPost the operation reads
   * @return what the API answered
   */
  public ServiceToken create(ServiceTokenPost body) {
    String path = "/api/v1/service_token/";
    List<QueryParameter> query = new ArrayList<>();
    return Problems.readAnswer(transport.request("POST", path, query, body.toJson()), ServiceToken::fromJson);
  }

  /**
   * Delete a service token
   *
   * @param serviceTokenId carries `service_token_id`.
   * @param organizationId carries `organization_id`.
   */
  public void delete(String serviceTokenId, String organizationId) {
    String path = "/api/v1/service_token/{service_token_id}";
    path = path.replace("{service_token_id}", Wire.pathSegment(serviceTokenId));
    List<QueryParameter> query = new ArrayList<>();
    query.add(new QueryParameter("organization_id", Wire.queryValue(organizationId)));
    Problems.checkAnswer(transport.request("DELETE", path, query, null));
  }

  /**
   * Edit a service token
   *
   * @param serviceTokenId carries `service_token_id`.
   * @param body the ServiceTokenPost the operation reads
   * @return what the API answered
   */
  public ServiceToken edit(String serviceTokenId, ServiceTokenPost body) {
    String path = "/api/v1/service_token/{service_token_id}";
    path = path.replace("{service_token_id}", Wire.pathSegment(serviceTokenId));
    List<QueryParameter> query = new ArrayList<>();
    return Problems.readAnswer(transport.request("PUT", path, query, body.toJson()), ServiceToken::fromJson);
  }

  /**
   * Get a service token
   *
   * @param serviceTokenId carries `service_token_id`.
   * @param organizationId carries `organization_id`.
   * @return what the API answered
   */
  public ServiceToken get(String serviceTokenId, String organizationId) {
    String path = "/api/v1/service_token/{service_token_id}";
    path = path.replace("{service_token_id}", Wire.pathSegment(serviceTokenId));
    List<QueryParameter> query = new ArrayList<>();
    query.add(new QueryParameter("organization_id", Wire.queryValue(organizationId)));
    return Problems.readAnswer(transport.request("GET", path, query, null), ServiceToken::fromJson);
  }

  /**
   * List service tokens
   *
   * @param organizationId carries `organization_id`.
   * @return what the API answered
   */
  public List<ServiceToken> list(String organizationId) {
    String path = "/api/v1/service_token/";
    List<QueryParameter> query = new ArrayList<>();
    query.add(new QueryParameter("organization_id", Wire.queryValue(organizationId)));
    return Problems.readAnswer(transport.request("GET", path, query, null), Wire.asList(ServiceToken::fromJson));
  }
}
