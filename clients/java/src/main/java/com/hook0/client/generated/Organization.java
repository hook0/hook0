// Generated by hook0-sdkgen from the OpenAPI snapshot the API crate commits.
// Do not edit by hand: run `UPDATE_SDK=java cargo test -p hook0-sdkgen sdk_targets` and commit the result.

package com.hook0.client.generated;

import com.hook0.client.Wire;
import java.util.LinkedHashMap;
import java.util.Map;
import java.util.UUID;

/**
 * The `Organization` the API declares.
 *
 * @param name carries `name`.
 * @param organizationId carries `organization_id`.
 * @param role carries `role`.
 * @param plan carries `plan`, or nothing when the API answers none.
 */
public record Organization(String name, UUID organizationId, String role, OrganizationPlan plan) {

  /**
   * Reads one out of what the API answered.
   *
   * @param value the JSON document the API answered
   * @return the Organization the API declares
   */
  public static Organization fromJson(Object value) {
    Map<String, Object> fields = Wire.asFields(value, "Organization");
    return new Organization(
        Wire.read(fields, "name", Wire::asText),
        Wire.read(fields, "organization_id", Wire::asUuid),
        Wire.read(fields, "role", Wire::asText),
        Wire.maybe(fields, "plan", OrganizationPlan::fromJson));
  }

  /**
   * Writes one back the way the API reads it.
   *
   * @return the document the API reads
   */
  public Map<String, Object> toJson() {
    Map<String, Object> out = new LinkedHashMap<>();
    out.put("name", name);
    out.put("organization_id", Wire.writeUuid(organizationId));
    out.put("role", role);
    if (plan != null) {
      out.put("plan", plan.toJson());
    }
    return out;
  }
}
