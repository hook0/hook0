// Generated by hook0-sdkgen from the OpenAPI snapshot the API crate commits.
// Do not edit by hand: run `UPDATE_SDK=java cargo test -p hook0-sdkgen sdk_targets` and commit the result.

package com.hook0.client.generated;

import com.hook0.client.QueryParameter;
import com.hook0.client.Transport;
import com.hook0.client.Wire;
import java.util.ArrayList;
import java.util.List;

/**
 * What the API declares under `subscriptions`, issued through the transport it is handed.
 *
 * Every call blocks until the API has answered.
 */
public final class SubscriptionsApi {

  private final Transport transport;

  /**
   * Builds the group on what its requests are issued through.
   *
   * @param transport what one request is issued through
   */
  public SubscriptionsApi(Transport transport) {
    this.transport = transport;
  }

  /**
   * Create a new subscription
   *
   * @param body the SubscriptionPost the operation reads
   * @return what the API answered
   */
  public Subscription create(SubscriptionPost body) {
    String path = "/api/v1/subscriptions/";
    List<QueryParameter> query = new ArrayList<>();
    return Problems.readAnswer(transport.request("POST", path, query, body.toJson()), Subscription::fromJson);
  }

  /**
   * Delete a subscription
   *
   * @param subscriptionId carries `subscription_id`.
   * @param applicationId carries `application_id`.
   */
  public void delete(String subscriptionId, String applicationId) {
    String path = "/api/v1/subscriptions/{subscription_id}";
    path = path.replace("{subscription_id}", Wire.pathSegment(subscriptionId));
    List<QueryParameter> query = new ArrayList<>();
    query.add(new QueryParameter("application_id", Wire.queryValue(applicationId)));
    Problems.checkAnswer(transport.request("DELETE", path, query, null));
  }

  /**
   * Get a subscription by its ID
   *
   * @param subscriptionId carries `subscription_id`.
   * @return what the API answered
   */
  public Subscription get(String subscriptionId) {
    String path = "/api/v1/subscriptions/{subscription_id}";
    path = path.replace("{subscription_id}", Wire.pathSegment(subscriptionId));
    List<QueryParameter> query = new ArrayList<>();
    return Problems.readAnswer(transport.request("GET", path, query, null), Subscription::fromJson);
  }

  /**
   * List subscriptions
   *
   * @param applicationId carries `application_id`.
   * @return what the API answered
   */
  public List<Subscription> list(String applicationId) {
    String path = "/api/v1/subscriptions/";
    List<QueryParameter> query = new ArrayList<>();
    query.add(new QueryParameter("application_id", Wire.queryValue(applicationId)));
    return Problems.readAnswer(transport.request("GET", path, query, null), Wire.asList(Subscription::fromJson));
  }

  /**
   * Update a subscription
   *
   * @param subscriptionId carries `subscription_id`.
   * @param body the SubscriptionPost the operation reads
   * @return what the API answered
   */
  public Subscription update(String subscriptionId, SubscriptionPost body) {
    String path = "/api/v1/subscriptions/{subscription_id}";
    path = path.replace("{subscription_id}", Wire.pathSegment(subscriptionId));
    List<QueryParameter> query = new ArrayList<>();
    return Problems.readAnswer(transport.request("PUT", path, query, body.toJson()), Subscription::fromJson);
  }
}
