// Generated by hook0-sdkgen from the OpenAPI snapshot the API crate commits.
// Do not edit by hand: run `UPDATE_SDK=java cargo test -p hook0-sdkgen sdk_targets` and commit the result.

package com.hook0.client.generated;

import com.hook0.client.QueryParameter;
import com.hook0.client.Transport;
import com.hook0.client.Wire;
import java.util.ArrayList;
import java.util.List;
import java.util.concurrent.CompletableFuture;

/**
 * What the API declares under `applications`, issued through the transport it is handed.
 *
 * Every call hands back what the API will answer.
 */
public final class ApplicationsAsyncApi {

  private final Transport transport;

  /**
   * Builds the group on what its requests are issued through.
   *
   * @param transport what one request is issued through
   */
  public ApplicationsAsyncApi(Transport transport) {
    this.transport = transport;
  }

  /**
   * Create a new application
   *
   * @param body the ApplicationPost the operation reads
   * @return what the API will answer
   */
  public CompletableFuture<Application> create(ApplicationPost body) {
    String path = "/api/v1/applications/";
    List<QueryParameter> query = new ArrayList<>();
    return transport.requestAsync("POST", path, query, body.toJson())
        .thenApply(Problems.readingWith(Application::fromJson));
  }

  /**
   * Delete an application
   *
   * @param applicationId carries `application_id`.
   * @return nothing, once the API has answered
   */
  public CompletableFuture<Void> delete(String applicationId) {
    String path = "/api/v1/applications/{application_id}";
    path = path.replace("{application_id}", Wire.pathSegment(applicationId));
    List<QueryParameter> query = new ArrayList<>();
    return transport.requestAsync("DELETE", path, query, null)
        .thenAccept(Problems::checkAnswer);
  }

  /**
   * Get an application by its ID
   *
   * @param applicationId carries `application_id`.
   * @return what the API will answer
   */
  public CompletableFuture<ApplicationInfo> get(String applicationId) {
    String path = "/api/v1/applications/{application_id}";
    path = path.replace("{application_id}", Wire.pathSegment(applicationId));
    List<QueryParameter> query = new ArrayList<>();
    return transport.requestAsync("GET", path, query, null)
        .thenApply(Problems.readingWith(ApplicationInfo::fromJson));
  }

  /**
   * List applications
   *
   * @param organizationId carries `organization_id`.
   * @return what the API will answer
   */
  public CompletableFuture<List<Application>> list(String organizationId) {
    String path = "/api/v1/applications/";
    List<QueryParameter> query = new ArrayList<>();
    query.add(new QueryParameter("organization_id", Wire.queryValue(organizationId)));
    return transport.requestAsync("GET", path, query, null)
        .thenApply(Problems.readingWith(Wire.asList(Application::fromJson)));
  }

  /**
   * Edit an application
   *
   * @param applicationId carries `application_id`.
   * @param body the ApplicationPost the operation reads
   * @return what the API will answer
   */
  public CompletableFuture<Application> update(String applicationId, ApplicationPost body) {
    String path = "/api/v1/applications/{application_id}";
    path = path.replace("{application_id}", Wire.pathSegment(applicationId));
    List<QueryParameter> query = new ArrayList<>();
    return transport.requestAsync("PUT", path, query, body.toJson())
        .thenApply(Problems.readingWith(Application::fromJson));
  }
}
