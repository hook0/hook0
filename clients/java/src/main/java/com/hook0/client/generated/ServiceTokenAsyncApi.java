// Generated by hook0-sdkgen from the OpenAPI snapshot the API crate commits.
// Do not edit by hand: run `UPDATE_SDK=java cargo test -p hook0-sdkgen sdk_targets` and commit the result.

package com.hook0.client.generated;

import com.hook0.client.QueryParameter;
import com.hook0.client.Transport;
import com.hook0.client.Wire;
import java.util.ArrayList;
import java.util.List;
import java.util.concurrent.CompletableFuture;

/**
 * What the API declares under `serviceToken`, issued through the transport it is handed.
 *
 * Every call hands back what the API will answer.
 */
public final class ServiceTokenAsyncApi {

  private final Transport transport;

  /**
   * Builds the group on what its requests are issued through.
   *
   * @param transport what one request is issued through
   */
  public ServiceTokenAsyncApi(Transport transport) {
    this.transport = transport;
  }

  /**
   * Create a new service token
   *
   * @param body the ServiceTokenPost the operation reads
   * @return what the API will answer
   */
  public CompletableFuture<ServiceToken> create(ServiceTokenPost body) {
    String path = "/api/v1/service_token/";
    List<QueryParameter> query = new ArrayList<>();
    return transport.requestAsync("POST", path, query, body.toJson())
        .thenApply(Problems.readingWith(ServiceToken::fromJson));
  }

  /**
   * Delete a service token
   *
   * @param serviceTokenId carries `service_token_id`.
   * @param organizationId carries `organization_id`.
   * @return nothing, once the API has answered
   */
  public CompletableFuture<Void> delete(String serviceTokenId, String organizationId) {
    String path = "/api/v1/service_token/{service_token_id}";
    path = path.replace("{service_token_id}", Wire.pathSegment(serviceTokenId));
    List<QueryParameter> query = new ArrayList<>();
    query.add(new QueryParameter("organization_id", Wire.queryValue(organizationId)));
    return transport.requestAsync("DELETE", path, query, null)
        .thenAccept(Problems::checkAnswer);
  }

  /**
   * Edit a service token
   *
   * @param serviceTokenId carries `service_token_id`.
   * @param body the ServiceTokenPost the operation reads
   * @return what the API will answer
   */
  public CompletableFuture<ServiceToken> edit(String serviceTokenId, ServiceTokenPost body) {
    String path = "/api/v1/service_token/{service_token_id}";
    path = path.replace("{service_token_id}", Wire.pathSegment(serviceTokenId));
    List<QueryParameter> query = new ArrayList<>();
    return transport.requestAsync("PUT", path, query, body.toJson())
        .thenApply(Problems.readingWith(ServiceToken::fromJson));
  }

  /**
   * Get a service token
   *
   * @param serviceTokenId carries `service_token_id`.
   * @param organizationId carries `organization_id`.
   * @return what the API will answer
   */
  public CompletableFuture<ServiceToken> get(String serviceTokenId, String organizationId) {
    String path = "/api/v1/service_token/{service_token_id}";
    path = path.replace("{service_token_id}", Wire.pathSegment(serviceTokenId));
    List<QueryParameter> query = new ArrayList<>();
    query.add(new QueryParameter("organization_id", Wire.queryValue(organizationId)));
    return transport.requestAsync("GET", path, query, null)
        .thenApply(Problems.readingWith(ServiceToken::fromJson));
  }

  /**
   * List service tokens
   *
   * @param organizationId carries `organization_id`.
   * @return what the API will answer
   */
  public CompletableFuture<List<ServiceToken>> list(String organizationId) {
    String path = "/api/v1/service_token/";
    List<QueryParameter> query = new ArrayList<>();
    query.add(new QueryParameter("organization_id", Wire.queryValue(organizationId)));
    return transport.requestAsync("GET", path, query, null)
        .thenApply(Problems.readingWith(Wire.asList(ServiceToken::fromJson)));
  }
}
