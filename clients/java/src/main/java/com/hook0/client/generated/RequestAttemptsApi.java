// Generated by hook0-sdkgen from the OpenAPI snapshot the API crate commits.
// Do not edit by hand: run `UPDATE_SDK=java cargo test -p hook0-sdkgen sdk_targets` and commit the result.

package com.hook0.client.generated;

import com.hook0.client.QueryParameter;
import com.hook0.client.Transport;
import com.hook0.client.Wire;
import java.util.ArrayList;
import java.util.List;

/**
 * What the API declares under `requestAttempts`, issued through the transport it is handed.
 *
 * Every call blocks until the API has answered.
 */
public final class RequestAttemptsApi {

  private final Transport transport;

  /**
   * Builds the group on what its requests are issued through.
   *
   * @param transport what one request is issued through
   */
  public RequestAttemptsApi(Transport transport) {
    this.transport = transport;
  }

  /**
   * Get a request attempt by its ID
   *
   * @param requestAttemptId carries `request_attempt_id`.
   * @param applicationId carries `application_id`.
   * @return what the API answered
   */
  public RequestAttempt get(String requestAttemptId, String applicationId) {
    String path = "/api/v1/request_attempts/{request_attempt_id}";
    path = path.replace("{request_attempt_id}", Wire.pathSegment(requestAttemptId));
    List<QueryParameter> query = new ArrayList<>();
    query.add(new QueryParameter("application_id", Wire.queryValue(applicationId)));
    return Problems.readAnswer(transport.request("GET", path, query, null), RequestAttempt::fromJson);
  }

  /**
   * List request attempts
   *
   * @param applicationId carries `application_id`.
   * @param eventEventTypeNames carries `event.event_type_names`, or nothing when the caller sends none: Comma-separated
   *     event types
   * @param eventId carries `event_id`, or nothing when the caller sends none.
   * @param maxCreatedAt carries `max_created_at`, or nothing when the caller sends none.
   * @param minCreatedAt carries `min_created_at`, or nothing when the caller sends none.
   * @param paginationCursor carries `pagination_cursor`, or nothing when the caller sends none.
   * @param subscriptionId carries `subscription_id`, or nothing when the caller sends none.
   * @return what the API answered
   */
  public List<RequestAttempt> list(
      String applicationId,
      String eventEventTypeNames,
      String eventId,
      String maxCreatedAt,
      String minCreatedAt,
      String paginationCursor,
      String subscriptionId) {
    String path = "/api/v1/request_attempts/";
    List<QueryParameter> query = new ArrayList<>();
    query.add(new QueryParameter("application_id", Wire.queryValue(applicationId)));
    if (eventEventTypeNames != null) {
      query.add(new QueryParameter("event.event_type_names", Wire.queryValue(eventEventTypeNames)));
    }
    if (eventId != null) {
      query.add(new QueryParameter("event_id", Wire.queryValue(eventId)));
    }
    if (maxCreatedAt != null) {
      query.add(new QueryParameter("max_created_at", Wire.queryValue(maxCreatedAt)));
    }
    if (minCreatedAt != null) {
      query.add(new QueryParameter("min_created_at", Wire.queryValue(minCreatedAt)));
    }
    if (paginationCursor != null) {
      query.add(new QueryParameter("pagination_cursor", Wire.queryValue(paginationCursor)));
    }
    if (subscriptionId != null) {
      query.add(new QueryParameter("subscription_id", Wire.queryValue(subscriptionId)));
    }
    return Problems.readAnswer(transport.request("GET", path, query, null), Wire.asList(RequestAttempt::fromJson));
  }
}
