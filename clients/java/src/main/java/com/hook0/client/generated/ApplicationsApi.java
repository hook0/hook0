// Generated by hook0-sdkgen from the OpenAPI snapshot the API crate commits.
// Do not edit by hand: run `UPDATE_SDK=java cargo test -p hook0-sdkgen sdk_targets` and commit the result.

package com.hook0.client.generated;

import com.hook0.client.QueryParameter;
import com.hook0.client.Transport;
import com.hook0.client.Wire;
import java.util.ArrayList;
import java.util.List;

/**
 * What the API declares under `applications`, issued through the transport it is handed.
 *
 * Every call blocks until the API has answered.
 */
public final class ApplicationsApi {

  private final Transport transport;

  /**
   * Builds the group on what its requests are issued through.
   *
   * @param transport what one request is issued through
   */
  public ApplicationsApi(Transport transport) {
    this.transport = transport;
  }

  /**
   * Create a new application
   *
   * @param body the ApplicationPost the operation reads
   * @return what the API answered
   */
  public Application create(ApplicationPost body) {
    String path = "/api/v1/applications/";
    List<QueryParameter> query = new ArrayList<>();
    return Problems.readAnswer(transport.request("POST", path, query, body.toJson()), Application::fromJson);
  }

  /**
   * Delete an application
   *
   * @param applicationId carries `application_id`.
   */
  public void delete(String applicationId) {
    String path = "/api/v1/applications/{application_id}";
    path = path.replace("{application_id}", Wire.pathSegment(applicationId));
    List<QueryParameter> query = new ArrayList<>();
    Problems.checkAnswer(transport.request("DELETE", path, query, null));
  }

  /**
   * Get an application by its ID
   *
   * @param applicationId carries `application_id`.
   * @return what the API answered
   */
  public ApplicationInfo get(String applicationId) {
    String path = "/api/v1/applications/{application_id}";
    path = path.replace("{application_id}", Wire.pathSegment(applicationId));
    List<QueryParameter> query = new ArrayList<>();
    return Problems.readAnswer(transport.request("GET", path, query, null), ApplicationInfo::fromJson);
  }

  /**
   * List applications
   *
   * @param organizationId carries `organization_id`.
   * @return what the API answered
   */
  public List<Application> list(String organizationId) {
    String path = "/api/v1/applications/";
    List<QueryParameter> query = new ArrayList<>();
    query.add(new QueryParameter("organization_id", Wire.queryValue(organizationId)));
    return Problems.readAnswer(transport.request("GET", path, query, null), Wire.asList(Application::fromJson));
  }

  /**
   * Edit an application
   *
   * @param applicationId carries `application_id`.
   * @param body the ApplicationPost the operation reads
   * @return what the API answered
   */
  public Application update(String applicationId, ApplicationPost body) {
    String path = "/api/v1/applications/{application_id}";
    path = path.replace("{application_id}", Wire.pathSegment(applicationId));
    List<QueryParameter> query = new ArrayList<>();
    return Problems.readAnswer(transport.request("PUT", path, query, body.toJson()), Application::fromJson);
  }
}
