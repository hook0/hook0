// Generated by hook0-sdkgen from the OpenAPI snapshot the API crate commits.
// Do not edit by hand: run `UPDATE_SDK=java cargo test -p hook0-sdkgen sdk_targets` and commit the result.

package com.hook0.client.generated;

import com.hook0.client.QueryParameter;
import com.hook0.client.Transport;
import com.hook0.client.Wire;
import java.util.ArrayList;
import java.util.List;
import java.util.concurrent.CompletableFuture;

/**
 * What the API declares under `applicationSecrets`, issued through the transport it is handed.
 *
 * Every call hands back what the API will answer.
 */
public final class ApplicationSecretsAsyncApi {

  private final Transport transport;

  /**
   * Builds the group on what its requests are issued through.
   *
   * @param transport what one request is issued through
   */
  public ApplicationSecretsAsyncApi(Transport transport) {
    this.transport = transport;
  }

  /**
   * Create a new application secret
   *
   * @param body the ApplicationSecretPost the operation reads
   * @return what the API will answer
   */
  public CompletableFuture<ApplicationSecret> create(ApplicationSecretPost body) {
    String path = "/api/v1/application_secrets/";
    List<QueryParameter> query = new ArrayList<>();
    return transport.requestAsync("POST", path, query, body.toJson())
        .thenApply(Problems.readingWith(ApplicationSecret::fromJson));
  }

  /**
   * Delete an application secret
   *
   * @param applicationSecretToken carries `application_secret_token`.
   * @param applicationId carries `application_id`.
   * @return nothing, once the API has answered
   */
  public CompletableFuture<Void> delete(String applicationSecretToken, String applicationId) {
    String path = "/api/v1/application_secrets/{application_secret_token}";
    path = path.replace("{application_secret_token}", Wire.pathSegment(applicationSecretToken));
    List<QueryParameter> query = new ArrayList<>();
    query.add(new QueryParameter("application_id", Wire.queryValue(applicationId)));
    return transport.requestAsync("DELETE", path, query, null)
        .thenAccept(Problems::checkAnswer);
  }

  /**
   * List application secrets
   *
   * @param applicationId carries `application_id`.
   * @return what the API will answer
   */
  public CompletableFuture<List<ApplicationSecret>> read(String applicationId) {
    String path = "/api/v1/application_secrets/";
    List<QueryParameter> query = new ArrayList<>();
    query.add(new QueryParameter("application_id", Wire.queryValue(applicationId)));
    return transport.requestAsync("GET", path, query, null)
        .thenApply(Problems.readingWith(Wire.asList(ApplicationSecret::fromJson)));
  }

  /**
   * Update an application secret
   *
   * @param applicationSecretToken carries `application_secret_token`.
   * @param body the ApplicationSecretPost the operation reads
   * @return what the API will answer
   */
  public CompletableFuture<ApplicationSecret> update(String applicationSecretToken, ApplicationSecretPost body) {
    String path = "/api/v1/application_secrets/{application_secret_token}";
    path = path.replace("{application_secret_token}", Wire.pathSegment(applicationSecretToken));
    List<QueryParameter> query = new ArrayList<>();
    return transport.requestAsync("PUT", path, query, body.toJson())
        .thenApply(Problems.readingWith(ApplicationSecret::fromJson));
  }
}
