// Generated by hook0-sdkgen from the OpenAPI snapshot the API crate commits.
// Do not edit by hand: run `UPDATE_SDK=zig cargo test -p hook0-sdkgen sdk_targets` and commit the result.

const std = @import("std");

const runtime = @import("../runtime.zig");
const errors = @import("errors.zig");
const models = @import("models.zig");

/// What the API declares under `applicationSecrets`, issued through the transport it is built on.
pub const ApplicationSecretsApi = struct {
    /// What one request is issued through.
    transport: runtime.Transport,
    /// Where what a failure of this group reported is read into.
    ///
    /// Not the allocator a call is handed: that one frees what the call allocated on its way
    /// out, and what the failure reported is read after the call has returned.
    allocator: std.mem.Allocator,
    /// What the last failure of this group reported, which an error alone cannot carry.
    reported: errors.Reported = .empty,

    pub fn init(
        allocator: std.mem.Allocator,
        transport: runtime.Transport,
    ) ApplicationSecretsApi {
        return .{ .allocator = allocator, .transport = transport };
    }

    /// Frees what the last failure of this group reported.
    pub fn deinit(self: *ApplicationSecretsApi) void {
        self.reported.deinit();
    }

    /// Create a new application secret
    pub fn create(
        self: *ApplicationSecretsApi,
        allocator: std.mem.Allocator,
        body: models.ApplicationSecretPost,
    ) !runtime.Owned(models.ApplicationSecret) {
        var owned: runtime.Owned(models.ApplicationSecret) = try .init(allocator);
        errdefer owned.deinit();
        const arena = owned.arena.allocator();

        const answered = try self.transport.request(arena, .{
            .method = "POST",
            .path = "/api/v1/application_secrets/",
            .body = try runtime.written(arena, body),
        });
        try errors.raiseForStatus(
            self.allocator,
            answered.status,
            answered.payload,
            &self.reported,
        );

        owned.value = try models.ApplicationSecret.fromJson(arena, try runtime.decodePayload(arena, answered.payload));
        return owned;
    }

    /// Delete an application secret
    /// `application_secret_token` carries `application_secret_token`.
    /// `application_id` carries `application_id`.
    pub fn delete(
        self: *ApplicationSecretsApi,
        allocator: std.mem.Allocator,
        application_secret_token: []const u8,
        application_id: []const u8,
    ) !void {
        var held: std.heap.ArenaAllocator = .init(allocator);
        defer held.deinit();
        const arena = held.allocator();

        const answered = try self.transport.request(arena, .{
            .method = "DELETE",
            .path = try runtime.path(arena, "/api/v1/application_secrets/{application_secret_token}", &.{
                .{ .name = "application_secret_token", .value = runtime.value(application_secret_token) },
            }),
            .query = &.{
                .{ .name = "application_id", .value = runtime.value(application_id) },
            },
        });
        try errors.raiseForStatus(
            self.allocator,
            answered.status,
            answered.payload,
            &self.reported,
        );
    }

    /// List application secrets
    /// `application_id` carries `application_id`.
    pub fn read(
        self: *ApplicationSecretsApi,
        allocator: std.mem.Allocator,
        application_id: []const u8,
    ) !runtime.Owned([]const models.ApplicationSecret) {
        var owned: runtime.Owned([]const models.ApplicationSecret) = try .init(allocator);
        errdefer owned.deinit();
        const arena = owned.arena.allocator();

        const answered = try self.transport.request(arena, .{
            .method = "GET",
            .path = "/api/v1/application_secrets/",
            .query = &.{
                .{ .name = "application_id", .value = runtime.value(application_id) },
            },
        });
        try errors.raiseForStatus(
            self.allocator,
            answered.status,
            answered.payload,
            &self.reported,
        );

        owned.value = try runtime.list(models.ApplicationSecret.fromJson).read(arena, try runtime.decodePayload(arena, answered.payload));
        return owned;
    }

    /// Update an application secret
    /// `application_secret_token` carries `application_secret_token`.
    pub fn update(
        self: *ApplicationSecretsApi,
        allocator: std.mem.Allocator,
        application_secret_token: []const u8,
        body: models.ApplicationSecretPost,
    ) !runtime.Owned(models.ApplicationSecret) {
        var owned: runtime.Owned(models.ApplicationSecret) = try .init(allocator);
        errdefer owned.deinit();
        const arena = owned.arena.allocator();

        const answered = try self.transport.request(arena, .{
            .method = "PUT",
            .path = try runtime.path(arena, "/api/v1/application_secrets/{application_secret_token}", &.{
                .{ .name = "application_secret_token", .value = runtime.value(application_secret_token) },
            }),
            .body = try runtime.written(arena, body),
        });
        try errors.raiseForStatus(
            self.allocator,
            answered.status,
            answered.payload,
            &self.reported,
        );

        owned.value = try models.ApplicationSecret.fromJson(arena, try runtime.decodePayload(arena, answered.payload));
        return owned;
    }
};

/// What the API declares under `applications`, issued through the transport it is built on.
pub const ApplicationsApi = struct {
    /// What one request is issued through.
    transport: runtime.Transport,
    /// Where what a failure of this group reported is read into.
    ///
    /// Not the allocator a call is handed: that one frees what the call allocated on its way
    /// out, and what the failure reported is read after the call has returned.
    allocator: std.mem.Allocator,
    /// What the last failure of this group reported, which an error alone cannot carry.
    reported: errors.Reported = .empty,

    pub fn init(
        allocator: std.mem.Allocator,
        transport: runtime.Transport,
    ) ApplicationsApi {
        return .{ .allocator = allocator, .transport = transport };
    }

    /// Frees what the last failure of this group reported.
    pub fn deinit(self: *ApplicationsApi) void {
        self.reported.deinit();
    }

    /// Create a new application
    pub fn create(
        self: *ApplicationsApi,
        allocator: std.mem.Allocator,
        body: models.ApplicationPost,
    ) !runtime.Owned(models.Application) {
        var owned: runtime.Owned(models.Application) = try .init(allocator);
        errdefer owned.deinit();
        const arena = owned.arena.allocator();

        const answered = try self.transport.request(arena, .{
            .method = "POST",
            .path = "/api/v1/applications/",
            .body = try runtime.written(arena, body),
        });
        try errors.raiseForStatus(
            self.allocator,
            answered.status,
            answered.payload,
            &self.reported,
        );

        owned.value = try models.Application.fromJson(arena, try runtime.decodePayload(arena, answered.payload));
        return owned;
    }

    /// Delete an application
    /// `application_id` carries `application_id`.
    pub fn delete(
        self: *ApplicationsApi,
        allocator: std.mem.Allocator,
        application_id: []const u8,
    ) !void {
        var held: std.heap.ArenaAllocator = .init(allocator);
        defer held.deinit();
        const arena = held.allocator();

        const answered = try self.transport.request(arena, .{
            .method = "DELETE",
            .path = try runtime.path(arena, "/api/v1/applications/{application_id}", &.{
                .{ .name = "application_id", .value = runtime.value(application_id) },
            }),
        });
        try errors.raiseForStatus(
            self.allocator,
            answered.status,
            answered.payload,
            &self.reported,
        );
    }

    /// Get an application by its ID
    /// `application_id` carries `application_id`.
    pub fn get(
        self: *ApplicationsApi,
        allocator: std.mem.Allocator,
        application_id: []const u8,
    ) !runtime.Owned(models.ApplicationInfo) {
        var owned: runtime.Owned(models.ApplicationInfo) = try .init(allocator);
        errdefer owned.deinit();
        const arena = owned.arena.allocator();

        const answered = try self.transport.request(arena, .{
            .method = "GET",
            .path = try runtime.path(arena, "/api/v1/applications/{application_id}", &.{
                .{ .name = "application_id", .value = runtime.value(application_id) },
            }),
        });
        try errors.raiseForStatus(
            self.allocator,
            answered.status,
            answered.payload,
            &self.reported,
        );

        owned.value = try models.ApplicationInfo.fromJson(arena, try runtime.decodePayload(arena, answered.payload));
        return owned;
    }

    /// List applications
    /// `organization_id` carries `organization_id`.
    pub fn list(
        self: *ApplicationsApi,
        allocator: std.mem.Allocator,
        organization_id: []const u8,
    ) !runtime.Owned([]const models.Application) {
        var owned: runtime.Owned([]const models.Application) = try .init(allocator);
        errdefer owned.deinit();
        const arena = owned.arena.allocator();

        const answered = try self.transport.request(arena, .{
            .method = "GET",
            .path = "/api/v1/applications/",
            .query = &.{
                .{ .name = "organization_id", .value = runtime.value(organization_id) },
            },
        });
        try errors.raiseForStatus(
            self.allocator,
            answered.status,
            answered.payload,
            &self.reported,
        );

        owned.value = try runtime.list(models.Application.fromJson).read(arena, try runtime.decodePayload(arena, answered.payload));
        return owned;
    }

    /// Edit an application
    /// `application_id` carries `application_id`.
    pub fn update(
        self: *ApplicationsApi,
        allocator: std.mem.Allocator,
        application_id: []const u8,
        body: models.ApplicationPost,
    ) !runtime.Owned(models.Application) {
        var owned: runtime.Owned(models.Application) = try .init(allocator);
        errdefer owned.deinit();
        const arena = owned.arena.allocator();

        const answered = try self.transport.request(arena, .{
            .method = "PUT",
            .path = try runtime.path(arena, "/api/v1/applications/{application_id}", &.{
                .{ .name = "application_id", .value = runtime.value(application_id) },
            }),
            .body = try runtime.written(arena, body),
        });
        try errors.raiseForStatus(
            self.allocator,
            answered.status,
            answered.payload,
            &self.reported,
        );

        owned.value = try models.Application.fromJson(arena, try runtime.decodePayload(arena, answered.payload));
        return owned;
    }
};

/// What the API declares under `errors`, issued through the transport it is built on.
pub const ErrorsApi = struct {
    /// What one request is issued through.
    transport: runtime.Transport,
    /// Where what a failure of this group reported is read into.
    ///
    /// Not the allocator a call is handed: that one frees what the call allocated on its way
    /// out, and what the failure reported is read after the call has returned.
    allocator: std.mem.Allocator,
    /// What the last failure of this group reported, which an error alone cannot carry.
    reported: errors.Reported = .empty,

    pub fn init(
        allocator: std.mem.Allocator,
        transport: runtime.Transport,
    ) ErrorsApi {
        return .{ .allocator = allocator, .transport = transport };
    }

    /// Frees what the last failure of this group reported.
    pub fn deinit(self: *ErrorsApi) void {
        self.reported.deinit();
    }

    /// List errors
    pub fn list(
        self: *ErrorsApi,
        allocator: std.mem.Allocator,
    ) !runtime.Owned([]const models.Problem) {
        var owned: runtime.Owned([]const models.Problem) = try .init(allocator);
        errdefer owned.deinit();
        const arena = owned.arena.allocator();

        const answered = try self.transport.request(arena, .{
            .method = "GET",
            .path = "/api/v1/errors/",
        });
        try errors.raiseForStatus(
            self.allocator,
            answered.status,
            answered.payload,
            &self.reported,
        );

        owned.value = try runtime.list(models.Problem.fromJson).read(arena, try runtime.decodePayload(arena, answered.payload));
        return owned;
    }
};

/// What the API declares under `eventTypes`, issued through the transport it is built on.
pub const EventTypesApi = struct {
    /// What one request is issued through.
    transport: runtime.Transport,
    /// Where what a failure of this group reported is read into.
    ///
    /// Not the allocator a call is handed: that one frees what the call allocated on its way
    /// out, and what the failure reported is read after the call has returned.
    allocator: std.mem.Allocator,
    /// What the last failure of this group reported, which an error alone cannot carry.
    reported: errors.Reported = .empty,

    pub fn init(
        allocator: std.mem.Allocator,
        transport: runtime.Transport,
    ) EventTypesApi {
        return .{ .allocator = allocator, .transport = transport };
    }

    /// Frees what the last failure of this group reported.
    pub fn deinit(self: *EventTypesApi) void {
        self.reported.deinit();
    }

    /// Create a new event type
    pub fn create(
        self: *EventTypesApi,
        allocator: std.mem.Allocator,
        body: models.EventTypePost,
    ) !runtime.Owned(models.EventType) {
        var owned: runtime.Owned(models.EventType) = try .init(allocator);
        errdefer owned.deinit();
        const arena = owned.arena.allocator();

        const answered = try self.transport.request(arena, .{
            .method = "POST",
            .path = "/api/v1/event_types/",
            .body = try runtime.written(arena, body),
        });
        try errors.raiseForStatus(
            self.allocator,
            answered.status,
            answered.payload,
            &self.reported,
        );

        owned.value = try models.EventType.fromJson(arena, try runtime.decodePayload(arena, answered.payload));
        return owned;
    }

    /// Delete an event type
    /// `event_type_name` carries `event_type_name`.
    /// `application_id` carries `application_id`.
    pub fn delete(
        self: *EventTypesApi,
        allocator: std.mem.Allocator,
        event_type_name: []const u8,
        application_id: []const u8,
    ) !void {
        var held: std.heap.ArenaAllocator = .init(allocator);
        defer held.deinit();
        const arena = held.allocator();

        const answered = try self.transport.request(arena, .{
            .method = "DELETE",
            .path = try runtime.path(arena, "/api/v1/event_types/{event_type_name}", &.{
                .{ .name = "event_type_name", .value = runtime.value(event_type_name) },
            }),
            .query = &.{
                .{ .name = "application_id", .value = runtime.value(application_id) },
            },
        });
        try errors.raiseForStatus(
            self.allocator,
            answered.status,
            answered.payload,
            &self.reported,
        );
    }

    /// Get an event type by its name
    /// `event_type_name` carries `event_type_name`.
    /// `application_id` carries `application_id`.
    pub fn get(
        self: *EventTypesApi,
        allocator: std.mem.Allocator,
        event_type_name: []const u8,
        application_id: []const u8,
    ) !runtime.Owned(models.EventType) {
        var owned: runtime.Owned(models.EventType) = try .init(allocator);
        errdefer owned.deinit();
        const arena = owned.arena.allocator();

        const answered = try self.transport.request(arena, .{
            .method = "GET",
            .path = try runtime.path(arena, "/api/v1/event_types/{event_type_name}", &.{
                .{ .name = "event_type_name", .value = runtime.value(event_type_name) },
            }),
            .query = &.{
                .{ .name = "application_id", .value = runtime.value(application_id) },
            },
        });
        try errors.raiseForStatus(
            self.allocator,
            answered.status,
            answered.payload,
            &self.reported,
        );

        owned.value = try models.EventType.fromJson(arena, try runtime.decodePayload(arena, answered.payload));
        return owned;
    }

    /// List event types
    /// `application_id` carries `application_id`.
    pub fn list(
        self: *EventTypesApi,
        allocator: std.mem.Allocator,
        application_id: []const u8,
    ) !runtime.Owned([]const models.EventType) {
        var owned: runtime.Owned([]const models.EventType) = try .init(allocator);
        errdefer owned.deinit();
        const arena = owned.arena.allocator();

        const answered = try self.transport.request(arena, .{
            .method = "GET",
            .path = "/api/v1/event_types/",
            .query = &.{
                .{ .name = "application_id", .value = runtime.value(application_id) },
            },
        });
        try errors.raiseForStatus(
            self.allocator,
            answered.status,
            answered.payload,
            &self.reported,
        );

        owned.value = try runtime.list(models.EventType.fromJson).read(arena, try runtime.decodePayload(arena, answered.payload));
        return owned;
    }
};

/// What the API declares under `events`, issued through the transport it is built on.
pub const EventsApi = struct {
    /// What one request is issued through.
    transport: runtime.Transport,
    /// Where what a failure of this group reported is read into.
    ///
    /// Not the allocator a call is handed: that one frees what the call allocated on its way
    /// out, and what the failure reported is read after the call has returned.
    allocator: std.mem.Allocator,
    /// What the last failure of this group reported, which an error alone cannot carry.
    reported: errors.Reported = .empty,

    pub fn init(
        allocator: std.mem.Allocator,
        transport: runtime.Transport,
    ) EventsApi {
        return .{ .allocator = allocator, .transport = transport };
    }

    /// Frees what the last failure of this group reported.
    pub fn deinit(self: *EventsApi) void {
        self.reported.deinit();
    }

    /// Get an event by its ID
    /// `event_id` carries `event_id`.
    /// `application_id` carries `application_id`.
    pub fn get(
        self: *EventsApi,
        allocator: std.mem.Allocator,
        event_id: []const u8,
        application_id: []const u8,
    ) !runtime.Owned(models.EventWithPayload) {
        var owned: runtime.Owned(models.EventWithPayload) = try .init(allocator);
        errdefer owned.deinit();
        const arena = owned.arena.allocator();

        const answered = try self.transport.request(arena, .{
            .method = "GET",
            .path = try runtime.path(arena, "/api/v1/events/{event_id}", &.{
                .{ .name = "event_id", .value = runtime.value(event_id) },
            }),
            .query = &.{
                .{ .name = "application_id", .value = runtime.value(application_id) },
            },
        });
        try errors.raiseForStatus(
            self.allocator,
            answered.status,
            answered.payload,
            &self.reported,
        );

        owned.value = try models.EventWithPayload.fromJson(arena, try runtime.decodePayload(arena, answered.payload));
        return owned;
    }

    /// Ingest an event
    pub fn ingest(
        self: *EventsApi,
        allocator: std.mem.Allocator,
        body: models.EventPost,
    ) !runtime.Owned(models.IngestedEvent) {
        var owned: runtime.Owned(models.IngestedEvent) = try .init(allocator);
        errdefer owned.deinit();
        const arena = owned.arena.allocator();

        const answered = try self.transport.request(arena, .{
            .method = "POST",
            .path = "/api/v1/event/",
            .body = try runtime.written(arena, body),
        });
        try errors.raiseForStatus(
            self.allocator,
            answered.status,
            answered.payload,
            &self.reported,
        );

        owned.value = try models.IngestedEvent.fromJson(arena, try runtime.decodePayload(arena, answered.payload));
        return owned;
    }

    /// List latest events
    /// `application_id` carries `application_id`.
    pub fn list(
        self: *EventsApi,
        allocator: std.mem.Allocator,
        application_id: []const u8,
    ) !runtime.Owned([]const models.Event) {
        var owned: runtime.Owned([]const models.Event) = try .init(allocator);
        errdefer owned.deinit();
        const arena = owned.arena.allocator();

        const answered = try self.transport.request(arena, .{
            .method = "GET",
            .path = "/api/v1/events/",
            .query = &.{
                .{ .name = "application_id", .value = runtime.value(application_id) },
            },
        });
        try errors.raiseForStatus(
            self.allocator,
            answered.status,
            answered.payload,
            &self.reported,
        );

        owned.value = try runtime.list(models.Event.fromJson).read(arena, try runtime.decodePayload(arena, answered.payload));
        return owned;
    }

    /// Replay an event
    /// `event_id` carries `event_id`.
    pub fn replay(
        self: *EventsApi,
        allocator: std.mem.Allocator,
        event_id: []const u8,
        body: models.ReplayEvent,
    ) !void {
        var held: std.heap.ArenaAllocator = .init(allocator);
        defer held.deinit();
        const arena = held.allocator();

        const answered = try self.transport.request(arena, .{
            .method = "POST",
            .path = try runtime.path(arena, "/api/v1/events/{event_id}/replay", &.{
                .{ .name = "event_id", .value = runtime.value(event_id) },
            }),
            .body = try runtime.written(arena, body),
        });
        try errors.raiseForStatus(
            self.allocator,
            answered.status,
            answered.payload,
            &self.reported,
        );
    }
};

/// What the API declares under `events_per_day`, issued through the transport it is built on.
pub const EventsPerDayApi = struct {
    /// What one request is issued through.
    transport: runtime.Transport,
    /// Where what a failure of this group reported is read into.
    ///
    /// Not the allocator a call is handed: that one frees what the call allocated on its way
    /// out, and what the failure reported is read after the call has returned.
    allocator: std.mem.Allocator,
    /// What the last failure of this group reported, which an error alone cannot carry.
    reported: errors.Reported = .empty,

    pub fn init(
        allocator: std.mem.Allocator,
        transport: runtime.Transport,
    ) EventsPerDayApi {
        return .{ .allocator = allocator, .transport = transport };
    }

    /// Frees what the last failure of this group reported.
    pub fn deinit(self: *EventsPerDayApi) void {
        self.reported.deinit();
    }

    /// List events per day for an application
    /// `application_id` carries `application_id`.
    /// `from` carries `from`: Start of date range (inclusive). Defaults to 30 days before `to`.
    /// `to` carries `to`: End of date range (inclusive). Defaults to today.
    pub fn listForApplication(
        self: *EventsPerDayApi,
        allocator: std.mem.Allocator,
        application_id: []const u8,
        from: ?[]const u8,
        to: ?[]const u8,
    ) !runtime.Owned([]const models.EventsPerDayEntry) {
        var owned: runtime.Owned([]const models.EventsPerDayEntry) = try .init(allocator);
        errdefer owned.deinit();
        const arena = owned.arena.allocator();

        const answered = try self.transport.request(arena, .{
            .method = "GET",
            .path = "/api/v1/events_per_day/application",
            .query = &.{
                .{ .name = "application_id", .value = runtime.value(application_id) },
                .{ .name = "from", .value = runtime.value(from) },
                .{ .name = "to", .value = runtime.value(to) },
            },
        });
        try errors.raiseForStatus(
            self.allocator,
            answered.status,
            answered.payload,
            &self.reported,
        );

        owned.value = try runtime.list(models.EventsPerDayEntry.fromJson).read(arena, try runtime.decodePayload(arena, answered.payload));
        return owned;
    }

    /// List events per day for an organization
    /// `organization_id` carries `organization_id`.
    /// `from` carries `from`: Start of date range (inclusive). Defaults to 30 days before `to`.
    /// `to` carries `to`: End of date range (inclusive). Defaults to today.
    pub fn listForOrganization(
        self: *EventsPerDayApi,
        allocator: std.mem.Allocator,
        organization_id: []const u8,
        from: ?[]const u8,
        to: ?[]const u8,
    ) !runtime.Owned([]const models.EventsPerDayEntry) {
        var owned: runtime.Owned([]const models.EventsPerDayEntry) = try .init(allocator);
        errdefer owned.deinit();
        const arena = owned.arena.allocator();

        const answered = try self.transport.request(arena, .{
            .method = "GET",
            .path = "/api/v1/events_per_day/organization",
            .query = &.{
                .{ .name = "organization_id", .value = runtime.value(organization_id) },
                .{ .name = "from", .value = runtime.value(from) },
                .{ .name = "to", .value = runtime.value(to) },
            },
        });
        try errors.raiseForStatus(
            self.allocator,
            answered.status,
            answered.payload,
            &self.reported,
        );

        owned.value = try runtime.list(models.EventsPerDayEntry.fromJson).read(arena, try runtime.decodePayload(arena, answered.payload));
        return owned;
    }
};

/// What the API declares under `instance`, issued through the transport it is built on.
pub const InstanceApi = struct {
    /// What one request is issued through.
    transport: runtime.Transport,
    /// Where what a failure of this group reported is read into.
    ///
    /// Not the allocator a call is handed: that one frees what the call allocated on its way
    /// out, and what the failure reported is read after the call has returned.
    allocator: std.mem.Allocator,
    /// What the last failure of this group reported, which an error alone cannot carry.
    reported: errors.Reported = .empty,

    pub fn init(
        allocator: std.mem.Allocator,
        transport: runtime.Transport,
    ) InstanceApi {
        return .{ .allocator = allocator, .transport = transport };
    }

    /// Frees what the last failure of this group reported.
    pub fn deinit(self: *InstanceApi) void {
        self.reported.deinit();
    }

    /// Get instance configuration
    pub fn get(
        self: *InstanceApi,
        allocator: std.mem.Allocator,
    ) !runtime.Owned(models.InstanceConfig) {
        var owned: runtime.Owned(models.InstanceConfig) = try .init(allocator);
        errdefer owned.deinit();
        const arena = owned.arena.allocator();

        const answered = try self.transport.request(arena, .{
            .method = "GET",
            .path = "/api/v1/instance/",
        });
        try errors.raiseForStatus(
            self.allocator,
            answered.status,
            answered.payload,
            &self.reported,
        );

        owned.value = try models.InstanceConfig.fromJson(arena, try runtime.decodePayload(arena, answered.payload));
        return owned;
    }
};

/// What the API declares under `payload_content_types`, issued through the transport it is built on.
pub const PayloadContentTypesApi = struct {
    /// What one request is issued through.
    transport: runtime.Transport,
    /// Where what a failure of this group reported is read into.
    ///
    /// Not the allocator a call is handed: that one frees what the call allocated on its way
    /// out, and what the failure reported is read after the call has returned.
    allocator: std.mem.Allocator,
    /// What the last failure of this group reported, which an error alone cannot carry.
    reported: errors.Reported = .empty,

    pub fn init(
        allocator: std.mem.Allocator,
        transport: runtime.Transport,
    ) PayloadContentTypesApi {
        return .{ .allocator = allocator, .transport = transport };
    }

    /// Frees what the last failure of this group reported.
    pub fn deinit(self: *PayloadContentTypesApi) void {
        self.reported.deinit();
    }

    /// List supported event payload content types
    pub fn list(
        self: *PayloadContentTypesApi,
        allocator: std.mem.Allocator,
    ) !runtime.Owned([]const []const u8) {
        var owned: runtime.Owned([]const []const u8) = try .init(allocator);
        errdefer owned.deinit();
        const arena = owned.arena.allocator();

        const answered = try self.transport.request(arena, .{
            .method = "GET",
            .path = "/api/v1/payload_content_types/",
        });
        try errors.raiseForStatus(
            self.allocator,
            answered.status,
            answered.payload,
            &self.reported,
        );

        owned.value = try runtime.list(runtime.text).read(arena, try runtime.decodePayload(arena, answered.payload));
        return owned;
    }
};

/// What the API declares under `quotas`, issued through the transport it is built on.
pub const QuotasApi = struct {
    /// What one request is issued through.
    transport: runtime.Transport,
    /// Where what a failure of this group reported is read into.
    ///
    /// Not the allocator a call is handed: that one frees what the call allocated on its way
    /// out, and what the failure reported is read after the call has returned.
    allocator: std.mem.Allocator,
    /// What the last failure of this group reported, which an error alone cannot carry.
    reported: errors.Reported = .empty,

    pub fn init(
        allocator: std.mem.Allocator,
        transport: runtime.Transport,
    ) QuotasApi {
        return .{ .allocator = allocator, .transport = transport };
    }

    /// Frees what the last failure of this group reported.
    pub fn deinit(self: *QuotasApi) void {
        self.reported.deinit();
    }

    /// Get quotas
    pub fn get(
        self: *QuotasApi,
        allocator: std.mem.Allocator,
    ) !runtime.Owned(models.QuotasResponse) {
        var owned: runtime.Owned(models.QuotasResponse) = try .init(allocator);
        errdefer owned.deinit();
        const arena = owned.arena.allocator();

        const answered = try self.transport.request(arena, .{
            .method = "GET",
            .path = "/api/v1/quotas/",
        });
        try errors.raiseForStatus(
            self.allocator,
            answered.status,
            answered.payload,
            &self.reported,
        );

        owned.value = try models.QuotasResponse.fromJson(arena, try runtime.decodePayload(arena, answered.payload));
        return owned;
    }
};

/// What the API declares under `requestAttempts`, issued through the transport it is built on.
pub const RequestAttemptsApi = struct {
    /// What one request is issued through.
    transport: runtime.Transport,
    /// Where what a failure of this group reported is read into.
    ///
    /// Not the allocator a call is handed: that one frees what the call allocated on its way
    /// out, and what the failure reported is read after the call has returned.
    allocator: std.mem.Allocator,
    /// What the last failure of this group reported, which an error alone cannot carry.
    reported: errors.Reported = .empty,

    pub fn init(
        allocator: std.mem.Allocator,
        transport: runtime.Transport,
    ) RequestAttemptsApi {
        return .{ .allocator = allocator, .transport = transport };
    }

    /// Frees what the last failure of this group reported.
    pub fn deinit(self: *RequestAttemptsApi) void {
        self.reported.deinit();
    }

    /// Get a request attempt by its ID
    /// `request_attempt_id` carries `request_attempt_id`.
    /// `application_id` carries `application_id`.
    pub fn get(
        self: *RequestAttemptsApi,
        allocator: std.mem.Allocator,
        request_attempt_id: []const u8,
        application_id: []const u8,
    ) !runtime.Owned(models.RequestAttempt) {
        var owned: runtime.Owned(models.RequestAttempt) = try .init(allocator);
        errdefer owned.deinit();
        const arena = owned.arena.allocator();

        const answered = try self.transport.request(arena, .{
            .method = "GET",
            .path = try runtime.path(arena, "/api/v1/request_attempts/{request_attempt_id}", &.{
                .{ .name = "request_attempt_id", .value = runtime.value(request_attempt_id) },
            }),
            .query = &.{
                .{ .name = "application_id", .value = runtime.value(application_id) },
            },
        });
        try errors.raiseForStatus(
            self.allocator,
            answered.status,
            answered.payload,
            &self.reported,
        );

        owned.value = try models.RequestAttempt.fromJson(arena, try runtime.decodePayload(arena, answered.payload));
        return owned;
    }

    /// List request attempts
    /// `application_id` carries `application_id`.
    /// `event_event_type_names` carries `event.event_type_names`: Comma-separated event types
    /// `event_id` carries `event_id`.
    /// `max_created_at` carries `max_created_at`.
    /// `min_created_at` carries `min_created_at`.
    /// `pagination_cursor` carries `pagination_cursor`.
    /// `subscription_id` carries `subscription_id`.
    pub fn read(
        self: *RequestAttemptsApi,
        allocator: std.mem.Allocator,
        application_id: []const u8,
        event_event_type_names: ?[]const u8,
        event_id: ?[]const u8,
        max_created_at: ?[]const u8,
        min_created_at: ?[]const u8,
        pagination_cursor: ?[]const u8,
        subscription_id: ?[]const u8,
    ) !runtime.Owned([]const models.RequestAttempt) {
        var owned: runtime.Owned([]const models.RequestAttempt) = try .init(allocator);
        errdefer owned.deinit();
        const arena = owned.arena.allocator();

        const answered = try self.transport.request(arena, .{
            .method = "GET",
            .path = "/api/v1/request_attempts/",
            .query = &.{
                .{ .name = "application_id", .value = runtime.value(application_id) },
                .{ .name = "event.event_type_names", .value = runtime.value(event_event_type_names) },
                .{ .name = "event_id", .value = runtime.value(event_id) },
                .{ .name = "max_created_at", .value = runtime.value(max_created_at) },
                .{ .name = "min_created_at", .value = runtime.value(min_created_at) },
                .{ .name = "pagination_cursor", .value = runtime.value(pagination_cursor) },
                .{ .name = "subscription_id", .value = runtime.value(subscription_id) },
            },
        });
        try errors.raiseForStatus(
            self.allocator,
            answered.status,
            answered.payload,
            &self.reported,
        );

        owned.value = try runtime.list(models.RequestAttempt.fromJson).read(arena, try runtime.decodePayload(arena, answered.payload));
        return owned;
    }
};

/// What the API declares under `response`, issued through the transport it is built on.
pub const ResponseApi = struct {
    /// What one request is issued through.
    transport: runtime.Transport,
    /// Where what a failure of this group reported is read into.
    ///
    /// Not the allocator a call is handed: that one frees what the call allocated on its way
    /// out, and what the failure reported is read after the call has returned.
    allocator: std.mem.Allocator,
    /// What the last failure of this group reported, which an error alone cannot carry.
    reported: errors.Reported = .empty,

    pub fn init(
        allocator: std.mem.Allocator,
        transport: runtime.Transport,
    ) ResponseApi {
        return .{ .allocator = allocator, .transport = transport };
    }

    /// Frees what the last failure of this group reported.
    pub fn deinit(self: *ResponseApi) void {
        self.reported.deinit();
    }

    /// Get a response by its ID
    /// `response_id` carries `response_id`.
    /// `application_id` carries `application_id`.
    pub fn get(
        self: *ResponseApi,
        allocator: std.mem.Allocator,
        response_id: []const u8,
        application_id: []const u8,
    ) !runtime.Owned(models.Response) {
        var owned: runtime.Owned(models.Response) = try .init(allocator);
        errdefer owned.deinit();
        const arena = owned.arena.allocator();

        const answered = try self.transport.request(arena, .{
            .method = "GET",
            .path = try runtime.path(arena, "/api/v1/responses/{response_id}", &.{
                .{ .name = "response_id", .value = runtime.value(response_id) },
            }),
            .query = &.{
                .{ .name = "application_id", .value = runtime.value(application_id) },
            },
        });
        try errors.raiseForStatus(
            self.allocator,
            answered.status,
            answered.payload,
            &self.reported,
        );

        owned.value = try models.Response.fromJson(arena, try runtime.decodePayload(arena, answered.payload));
        return owned;
    }
};

/// What the API declares under `serviceToken`, issued through the transport it is built on.
pub const ServiceTokenApi = struct {
    /// What one request is issued through.
    transport: runtime.Transport,
    /// Where what a failure of this group reported is read into.
    ///
    /// Not the allocator a call is handed: that one frees what the call allocated on its way
    /// out, and what the failure reported is read after the call has returned.
    allocator: std.mem.Allocator,
    /// What the last failure of this group reported, which an error alone cannot carry.
    reported: errors.Reported = .empty,

    pub fn init(
        allocator: std.mem.Allocator,
        transport: runtime.Transport,
    ) ServiceTokenApi {
        return .{ .allocator = allocator, .transport = transport };
    }

    /// Frees what the last failure of this group reported.
    pub fn deinit(self: *ServiceTokenApi) void {
        self.reported.deinit();
    }

    /// Create a new service token
    pub fn create(
        self: *ServiceTokenApi,
        allocator: std.mem.Allocator,
        body: models.ServiceTokenPost,
    ) !runtime.Owned(models.ServiceToken) {
        var owned: runtime.Owned(models.ServiceToken) = try .init(allocator);
        errdefer owned.deinit();
        const arena = owned.arena.allocator();

        const answered = try self.transport.request(arena, .{
            .method = "POST",
            .path = "/api/v1/service_token/",
            .body = try runtime.written(arena, body),
        });
        try errors.raiseForStatus(
            self.allocator,
            answered.status,
            answered.payload,
            &self.reported,
        );

        owned.value = try models.ServiceToken.fromJson(arena, try runtime.decodePayload(arena, answered.payload));
        return owned;
    }

    /// Delete a service token
    /// `service_token_id` carries `service_token_id`.
    /// `organization_id` carries `organization_id`.
    pub fn delete(
        self: *ServiceTokenApi,
        allocator: std.mem.Allocator,
        service_token_id: []const u8,
        organization_id: []const u8,
    ) !void {
        var held: std.heap.ArenaAllocator = .init(allocator);
        defer held.deinit();
        const arena = held.allocator();

        const answered = try self.transport.request(arena, .{
            .method = "DELETE",
            .path = try runtime.path(arena, "/api/v1/service_token/{service_token_id}", &.{
                .{ .name = "service_token_id", .value = runtime.value(service_token_id) },
            }),
            .query = &.{
                .{ .name = "organization_id", .value = runtime.value(organization_id) },
            },
        });
        try errors.raiseForStatus(
            self.allocator,
            answered.status,
            answered.payload,
            &self.reported,
        );
    }

    /// Edit a service token
    /// `service_token_id` carries `service_token_id`.
    pub fn edit(
        self: *ServiceTokenApi,
        allocator: std.mem.Allocator,
        service_token_id: []const u8,
        body: models.ServiceTokenPost,
    ) !runtime.Owned(models.ServiceToken) {
        var owned: runtime.Owned(models.ServiceToken) = try .init(allocator);
        errdefer owned.deinit();
        const arena = owned.arena.allocator();

        const answered = try self.transport.request(arena, .{
            .method = "PUT",
            .path = try runtime.path(arena, "/api/v1/service_token/{service_token_id}", &.{
                .{ .name = "service_token_id", .value = runtime.value(service_token_id) },
            }),
            .body = try runtime.written(arena, body),
        });
        try errors.raiseForStatus(
            self.allocator,
            answered.status,
            answered.payload,
            &self.reported,
        );

        owned.value = try models.ServiceToken.fromJson(arena, try runtime.decodePayload(arena, answered.payload));
        return owned;
    }

    /// Get a service token
    /// `service_token_id` carries `service_token_id`.
    /// `organization_id` carries `organization_id`.
    pub fn get(
        self: *ServiceTokenApi,
        allocator: std.mem.Allocator,
        service_token_id: []const u8,
        organization_id: []const u8,
    ) !runtime.Owned(models.ServiceToken) {
        var owned: runtime.Owned(models.ServiceToken) = try .init(allocator);
        errdefer owned.deinit();
        const arena = owned.arena.allocator();

        const answered = try self.transport.request(arena, .{
            .method = "GET",
            .path = try runtime.path(arena, "/api/v1/service_token/{service_token_id}", &.{
                .{ .name = "service_token_id", .value = runtime.value(service_token_id) },
            }),
            .query = &.{
                .{ .name = "organization_id", .value = runtime.value(organization_id) },
            },
        });
        try errors.raiseForStatus(
            self.allocator,
            answered.status,
            answered.payload,
            &self.reported,
        );

        owned.value = try models.ServiceToken.fromJson(arena, try runtime.decodePayload(arena, answered.payload));
        return owned;
    }

    /// List service tokens
    /// `organization_id` carries `organization_id`.
    pub fn list(
        self: *ServiceTokenApi,
        allocator: std.mem.Allocator,
        organization_id: []const u8,
    ) !runtime.Owned([]const models.ServiceToken) {
        var owned: runtime.Owned([]const models.ServiceToken) = try .init(allocator);
        errdefer owned.deinit();
        const arena = owned.arena.allocator();

        const answered = try self.transport.request(arena, .{
            .method = "GET",
            .path = "/api/v1/service_token/",
            .query = &.{
                .{ .name = "organization_id", .value = runtime.value(organization_id) },
            },
        });
        try errors.raiseForStatus(
            self.allocator,
            answered.status,
            answered.payload,
            &self.reported,
        );

        owned.value = try runtime.list(models.ServiceToken.fromJson).read(arena, try runtime.decodePayload(arena, answered.payload));
        return owned;
    }
};

/// What the API declares under `subscriptions`, issued through the transport it is built on.
pub const SubscriptionsApi = struct {
    /// What one request is issued through.
    transport: runtime.Transport,
    /// Where what a failure of this group reported is read into.
    ///
    /// Not the allocator a call is handed: that one frees what the call allocated on its way
    /// out, and what the failure reported is read after the call has returned.
    allocator: std.mem.Allocator,
    /// What the last failure of this group reported, which an error alone cannot carry.
    reported: errors.Reported = .empty,

    pub fn init(
        allocator: std.mem.Allocator,
        transport: runtime.Transport,
    ) SubscriptionsApi {
        return .{ .allocator = allocator, .transport = transport };
    }

    /// Frees what the last failure of this group reported.
    pub fn deinit(self: *SubscriptionsApi) void {
        self.reported.deinit();
    }

    /// Create a new subscription
    pub fn create(
        self: *SubscriptionsApi,
        allocator: std.mem.Allocator,
        body: models.SubscriptionPost,
    ) !runtime.Owned(models.Subscription) {
        var owned: runtime.Owned(models.Subscription) = try .init(allocator);
        errdefer owned.deinit();
        const arena = owned.arena.allocator();

        const answered = try self.transport.request(arena, .{
            .method = "POST",
            .path = "/api/v1/subscriptions/",
            .body = try runtime.written(arena, body),
        });
        try errors.raiseForStatus(
            self.allocator,
            answered.status,
            answered.payload,
            &self.reported,
        );

        owned.value = try models.Subscription.fromJson(arena, try runtime.decodePayload(arena, answered.payload));
        return owned;
    }

    /// Delete a subscription
    /// `subscription_id` carries `subscription_id`.
    /// `application_id` carries `application_id`.
    pub fn delete(
        self: *SubscriptionsApi,
        allocator: std.mem.Allocator,
        subscription_id: []const u8,
        application_id: []const u8,
    ) !void {
        var held: std.heap.ArenaAllocator = .init(allocator);
        defer held.deinit();
        const arena = held.allocator();

        const answered = try self.transport.request(arena, .{
            .method = "DELETE",
            .path = try runtime.path(arena, "/api/v1/subscriptions/{subscription_id}", &.{
                .{ .name = "subscription_id", .value = runtime.value(subscription_id) },
            }),
            .query = &.{
                .{ .name = "application_id", .value = runtime.value(application_id) },
            },
        });
        try errors.raiseForStatus(
            self.allocator,
            answered.status,
            answered.payload,
            &self.reported,
        );
    }

    /// Get a subscription by its ID
    /// `subscription_id` carries `subscription_id`.
    pub fn get(
        self: *SubscriptionsApi,
        allocator: std.mem.Allocator,
        subscription_id: []const u8,
    ) !runtime.Owned(models.Subscription) {
        var owned: runtime.Owned(models.Subscription) = try .init(allocator);
        errdefer owned.deinit();
        const arena = owned.arena.allocator();

        const answered = try self.transport.request(arena, .{
            .method = "GET",
            .path = try runtime.path(arena, "/api/v1/subscriptions/{subscription_id}", &.{
                .{ .name = "subscription_id", .value = runtime.value(subscription_id) },
            }),
        });
        try errors.raiseForStatus(
            self.allocator,
            answered.status,
            answered.payload,
            &self.reported,
        );

        owned.value = try models.Subscription.fromJson(arena, try runtime.decodePayload(arena, answered.payload));
        return owned;
    }

    /// List subscriptions
    /// `application_id` carries `application_id`.
    pub fn list(
        self: *SubscriptionsApi,
        allocator: std.mem.Allocator,
        application_id: []const u8,
    ) !runtime.Owned([]const models.Subscription) {
        var owned: runtime.Owned([]const models.Subscription) = try .init(allocator);
        errdefer owned.deinit();
        const arena = owned.arena.allocator();

        const answered = try self.transport.request(arena, .{
            .method = "GET",
            .path = "/api/v1/subscriptions/",
            .query = &.{
                .{ .name = "application_id", .value = runtime.value(application_id) },
            },
        });
        try errors.raiseForStatus(
            self.allocator,
            answered.status,
            answered.payload,
            &self.reported,
        );

        owned.value = try runtime.list(models.Subscription.fromJson).read(arena, try runtime.decodePayload(arena, answered.payload));
        return owned;
    }

    /// Update a subscription
    /// `subscription_id` carries `subscription_id`.
    pub fn update(
        self: *SubscriptionsApi,
        allocator: std.mem.Allocator,
        subscription_id: []const u8,
        body: models.SubscriptionPost,
    ) !runtime.Owned(models.Subscription) {
        var owned: runtime.Owned(models.Subscription) = try .init(allocator);
        errdefer owned.deinit();
        const arena = owned.arena.allocator();

        const answered = try self.transport.request(arena, .{
            .method = "PUT",
            .path = try runtime.path(arena, "/api/v1/subscriptions/{subscription_id}", &.{
                .{ .name = "subscription_id", .value = runtime.value(subscription_id) },
            }),
            .body = try runtime.written(arena, body),
        });
        try errors.raiseForStatus(
            self.allocator,
            answered.status,
            answered.payload,
            &self.reported,
        );

        owned.value = try models.Subscription.fromJson(arena, try runtime.decodePayload(arena, answered.payload));
        return owned;
    }
};
