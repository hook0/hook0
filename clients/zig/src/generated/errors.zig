// Generated by hook0-sdkgen from the OpenAPI snapshot the API crate commits.
// Do not edit by hand: run `UPDATE_SDK=zig cargo test -p hook0-sdkgen sdk_targets` and commit the result.

const std = @import("std");

const runtime = @import("../runtime.zig");
const models = @import("models.zig");

/// Every problem the API can report, one error each.
///
/// A Zig error carries no value, so what the API said beyond which problem it was — the
/// status it answered under and the document it answered — is written into the
/// [Reported] the caller handed in. That is the same separation Go's clients make
/// between the error and the value carrying it, rather than the exception hierarchy the
/// languages with one use.
pub const Failure = error{
    ApplicationNameMissing,
    AuthApplicationSecretLookupError,
    AuthBiscuitLookupError,
    AuthEmailAlreadyVerified,
    AuthEmailExpired,
    AuthEmailNotVerified,
    AuthFailedLogin,
    AuthFailedRefresh,
    AuthInvalidApplicationSecret,
    AuthInvalidAuthorizationHeader,
    AuthInvalidBiscuit,
    AuthNoAuthorizationHeader,
    EventAlreadyIngested,
    EventInvalidBase64Payload,
    EventInvalidJsonPayload,
    EventInvalidPayloadContentType,
    EventTypeAlreadyExist,
    EventTypeDoesNotExist,
    Forbidden,
    InternalServerError,
    InvalidDateRange,
    InvalidRole,
    InvitedUserAlreadyInOrganization,
    InvitedUserDoesNotExist,
    JsonPayload,
    LabelsAmbiguity,
    NotFound,
    OrganizationIsNotEmpty,
    OrganizationNameMissing,
    PasswordNotDiverseEnough,
    PasswordSimilarToEmail,
    PasswordSimilarToName,
    PasswordTooCommon,
    PasswordTooLong,
    PasswordTooShort,
    RateLimited,
    RegistrationDisabled,
    ServiceUnavailable,
    TooManyApplicationsPerOrganization,
    TooManyEventTypesPerApplication,
    TooManyEventsToday,
    TooManyMembersPerOrganization,
    TooManySubscriptionsPerApplication,
    UnauthorizedWorkers,
    UserAlreadyExist,
    Validation,
    /// The API answered a failure whose body names no problem this client knows.
    Unreadable,
};

/// What one failure of the API said about itself, beyond which error it is.
///
/// Everything it points into is held in an arena of its own, which is what lets it be read
/// after the call that drew it has freed everything that call allocated. The next failure
/// written here frees what the last one held, and `deinit` frees the last one of all.
pub const Reported = struct {
    /// What the API answered under.
    status: u16 = 0,
    /// The problem document it answered, when this client could read one.
    problem: ?models.Problem = null,
    /// What to say about the failure.
    detail: []const u8 = "",
    /// Where `problem` and `detail` were read into, which nothing else refers to.
    held: ?runtime.Kept = null,

    /// What a group carries before anything has failed.
    pub const empty: Reported = .{};

    /// Frees what the last failure reported, leaving this as it started.
    pub fn deinit(self: *Reported) void {
        if (self.held) |held| held.deinit();
        self.* = .empty;
    }
};

/// The error each problem the API names is answered as.
pub const problems = [_]struct { id: []const u8, raised: Failure }{
    .{ .id = models.ProblemId.application_name_missing, .raised = error.ApplicationNameMissing },
    .{ .id = models.ProblemId.auth_application_secret_lookup_error, .raised = error.AuthApplicationSecretLookupError },
    .{ .id = models.ProblemId.auth_biscuit_lookup_error, .raised = error.AuthBiscuitLookupError },
    .{ .id = models.ProblemId.auth_email_already_verified, .raised = error.AuthEmailAlreadyVerified },
    .{ .id = models.ProblemId.auth_email_expired, .raised = error.AuthEmailExpired },
    .{ .id = models.ProblemId.auth_email_not_verified, .raised = error.AuthEmailNotVerified },
    .{ .id = models.ProblemId.auth_failed_login, .raised = error.AuthFailedLogin },
    .{ .id = models.ProblemId.auth_failed_refresh, .raised = error.AuthFailedRefresh },
    .{ .id = models.ProblemId.auth_invalid_application_secret, .raised = error.AuthInvalidApplicationSecret },
    .{ .id = models.ProblemId.auth_invalid_authorization_header, .raised = error.AuthInvalidAuthorizationHeader },
    .{ .id = models.ProblemId.auth_invalid_biscuit, .raised = error.AuthInvalidBiscuit },
    .{ .id = models.ProblemId.auth_no_authorization_header, .raised = error.AuthNoAuthorizationHeader },
    .{ .id = models.ProblemId.event_already_ingested, .raised = error.EventAlreadyIngested },
    .{ .id = models.ProblemId.event_invalid_base64_payload, .raised = error.EventInvalidBase64Payload },
    .{ .id = models.ProblemId.event_invalid_json_payload, .raised = error.EventInvalidJsonPayload },
    .{ .id = models.ProblemId.event_invalid_payload_content_type, .raised = error.EventInvalidPayloadContentType },
    .{ .id = models.ProblemId.event_type_already_exist, .raised = error.EventTypeAlreadyExist },
    .{ .id = models.ProblemId.event_type_does_not_exist, .raised = error.EventTypeDoesNotExist },
    .{ .id = models.ProblemId.forbidden, .raised = error.Forbidden },
    .{ .id = models.ProblemId.internal_server_error, .raised = error.InternalServerError },
    .{ .id = models.ProblemId.invalid_date_range, .raised = error.InvalidDateRange },
    .{ .id = models.ProblemId.invalid_role, .raised = error.InvalidRole },
    .{ .id = models.ProblemId.invited_user_already_in_organization, .raised = error.InvitedUserAlreadyInOrganization },
    .{ .id = models.ProblemId.invited_user_does_not_exist, .raised = error.InvitedUserDoesNotExist },
    .{ .id = models.ProblemId.json_payload, .raised = error.JsonPayload },
    .{ .id = models.ProblemId.labels_ambiguity, .raised = error.LabelsAmbiguity },
    .{ .id = models.ProblemId.not_found, .raised = error.NotFound },
    .{ .id = models.ProblemId.organization_is_not_empty, .raised = error.OrganizationIsNotEmpty },
    .{ .id = models.ProblemId.organization_name_missing, .raised = error.OrganizationNameMissing },
    .{ .id = models.ProblemId.password_not_diverse_enough, .raised = error.PasswordNotDiverseEnough },
    .{ .id = models.ProblemId.password_similar_to_email, .raised = error.PasswordSimilarToEmail },
    .{ .id = models.ProblemId.password_similar_to_name, .raised = error.PasswordSimilarToName },
    .{ .id = models.ProblemId.password_too_common, .raised = error.PasswordTooCommon },
    .{ .id = models.ProblemId.password_too_long, .raised = error.PasswordTooLong },
    .{ .id = models.ProblemId.password_too_short, .raised = error.PasswordTooShort },
    .{ .id = models.ProblemId.rate_limited, .raised = error.RateLimited },
    .{ .id = models.ProblemId.registration_disabled, .raised = error.RegistrationDisabled },
    .{ .id = models.ProblemId.service_unavailable, .raised = error.ServiceUnavailable },
    .{ .id = models.ProblemId.too_many_applications_per_organization, .raised = error.TooManyApplicationsPerOrganization },
    .{ .id = models.ProblemId.too_many_event_types_per_application, .raised = error.TooManyEventTypesPerApplication },
    .{ .id = models.ProblemId.too_many_events_today, .raised = error.TooManyEventsToday },
    .{ .id = models.ProblemId.too_many_members_per_organization, .raised = error.TooManyMembersPerOrganization },
    .{ .id = models.ProblemId.too_many_subscriptions_per_application, .raised = error.TooManySubscriptionsPerApplication },
    .{ .id = models.ProblemId.unauthorized_workers, .raised = error.UnauthorizedWorkers },
    .{ .id = models.ProblemId.user_already_exist, .raised = error.UserAlreadyExist },
    .{ .id = models.ProblemId.validation, .raised = error.Validation },
};

/// The error a problem document names, when what it names is one this client knows.
fn raisedBy(problem: ?models.Problem) ?Failure {
    const named = problem orelse return null;
    for (problems) |entry| {
        if (std.mem.eql(u8, entry.id, named.id)) return entry.raised;
    }
    return null;
}

/// What one failure said about itself, read into an arena of its own.
///
/// The body is read where it lies and nothing points back into it afterwards: every
/// string the document carries is copied out of it as it is parsed, and the message is
/// written out. Both land in the arena this answers, and the body stays the call's.
fn reportedOf(
    allocator: std.mem.Allocator,
    status: u16,
    payload: []const u8,
) std.mem.Allocator.Error!Reported {
    const held: runtime.Kept = try .init(allocator);
    errdefer held.deinit();

    const into = held.arena.allocator();
    const problem = runtime.problemOf(models.Problem, into, payload);
    const detail = if (raisedBy(problem) != null)
        try runtime.reported(into, status, payload)
    else
        try runtime.unreadable(into, status, payload);

    return .{ .status = status, .problem = problem, .detail = detail, .held = held };
}

/// Answer what the API reported, when what it answered was not a success.
///
/// What the failure said about itself is written into `reported`, which is what a
/// caller reads once the error has told it there is something to read. The allocator is
/// the one that memory is taken from, and it has to be one outliving the call: what the
/// call itself allocated is gone by the time the caller reads any of this.
pub fn raiseForStatus(
    allocator: std.mem.Allocator,
    status: u16,
    payload: []const u8,
    reported: *Reported,
) (Failure || std.mem.Allocator.Error)!void {
    if (status >= 200 and status < 300) return;

    const read = try reportedOf(allocator, status, payload);
    reported.deinit();
    reported.* = read;

    return raisedBy(read.problem) orelse error.Unreadable;
}
