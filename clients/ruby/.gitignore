# What SimpleCov writes when the suite runs. It is the report of one run, not something a checkout
# should carry — and without this rule it lands in the working tree the first time anybody runs the
# tests, ready to be swept into an unrelated commit.
coverage/
