# Generated by hook0-sdkgen from the OpenAPI snapshot the API crate commits.
# Do not edit by hand: run `UPDATE_SDK=python cargo test -p hook0-sdkgen sdk_targets` and commit the result.
"""One method per operation the API declares, grouped by entity. This layer awaits its transport."""

from __future__ import annotations

from typing import Any

from ..runtime import (
    as_list,
    as_text,
    decode_payload,
    path_segment,
    query_value,
)
from .errors import raise_for_status
from .models import (
    Application,
    ApplicationInfo,
    ApplicationPost,
    ApplicationSecret,
    ApplicationSecretPost,
    Event,
    EventPost,
    EventType,
    EventTypePost,
    EventWithPayload,
    EventsPerDayEntry,
    IngestedEvent,
    InstanceConfig,
    Problem,
    QuotasResponse,
    ReplayEvent,
    RequestAttempt,
    Response,
    ServiceToken,
    ServiceTokenPost,
    Subscription,
    SubscriptionPost,
)


class ApplicationSecretsAsyncApi:
    """What the API declares under `applicationSecrets`, issued through the transport it is handed."""

    def __init__(self, transport: Any) -> None:
        self._transport = transport

    async def create(
        self,
        body: ApplicationSecretPost,
    ) -> ApplicationSecret:
        """Create a new application secret"""
        path = "/api/v1/application_secrets/"
        query: list[tuple[str, str]] = []
        status, payload = await self._transport.request(
            "POST",
            path,
            query,
            body.to_json(),
        )
        raise_for_status(status, payload)
        return ApplicationSecret.from_json(decode_payload(payload))

    async def delete(
        self,
        application_secret_token: str,
        application_id: str,
    ) -> None:
        """Delete an application secret"""
        path = "/api/v1/application_secrets/{application_secret_token}"
        path = path.replace("{application_secret_token}", path_segment(application_secret_token))
        query: list[tuple[str, str]] = []
        query.append(("application_id", query_value(application_id)))
        status, payload = await self._transport.request(
            "DELETE",
            path,
            query,
            None,
        )
        raise_for_status(status, payload)
        return None

    async def read(
        self,
        application_id: str,
    ) -> list[ApplicationSecret]:
        """List application secrets"""
        path = "/api/v1/application_secrets/"
        query: list[tuple[str, str]] = []
        query.append(("application_id", query_value(application_id)))
        status, payload = await self._transport.request(
            "GET",
            path,
            query,
            None,
        )
        raise_for_status(status, payload)
        return as_list(ApplicationSecret.from_json)(decode_payload(payload))

    async def update(
        self,
        application_secret_token: str,
        body: ApplicationSecretPost,
    ) -> ApplicationSecret:
        """Update an application secret"""
        path = "/api/v1/application_secrets/{application_secret_token}"
        path = path.replace("{application_secret_token}", path_segment(application_secret_token))
        query: list[tuple[str, str]] = []
        status, payload = await self._transport.request(
            "PUT",
            path,
            query,
            body.to_json(),
        )
        raise_for_status(status, payload)
        return ApplicationSecret.from_json(decode_payload(payload))


class ApplicationsAsyncApi:
    """What the API declares under `applications`, issued through the transport it is handed."""

    def __init__(self, transport: Any) -> None:
        self._transport = transport

    async def create(
        self,
        body: ApplicationPost,
    ) -> Application:
        """Create a new application"""
        path = "/api/v1/applications/"
        query: list[tuple[str, str]] = []
        status, payload = await self._transport.request(
            "POST",
            path,
            query,
            body.to_json(),
        )
        raise_for_status(status, payload)
        return Application.from_json(decode_payload(payload))

    async def delete(
        self,
        application_id: str,
    ) -> None:
        """Delete an application"""
        path = "/api/v1/applications/{application_id}"
        path = path.replace("{application_id}", path_segment(application_id))
        query: list[tuple[str, str]] = []
        status, payload = await self._transport.request(
            "DELETE",
            path,
            query,
            None,
        )
        raise_for_status(status, payload)
        return None

    async def get(
        self,
        application_id: str,
    ) -> ApplicationInfo:
        """Get an application by its ID"""
        path = "/api/v1/applications/{application_id}"
        path = path.replace("{application_id}", path_segment(application_id))
        query: list[tuple[str, str]] = []
        status, payload = await self._transport.request(
            "GET",
            path,
            query,
            None,
        )
        raise_for_status(status, payload)
        return ApplicationInfo.from_json(decode_payload(payload))

    async def list(
        self,
        organization_id: str,
    ) -> list[Application]:
        """List applications"""
        path = "/api/v1/applications/"
        query: list[tuple[str, str]] = []
        query.append(("organization_id", query_value(organization_id)))
        status, payload = await self._transport.request(
            "GET",
            path,
            query,
            None,
        )
        raise_for_status(status, payload)
        return as_list(Application.from_json)(decode_payload(payload))

    async def update(
        self,
        application_id: str,
        body: ApplicationPost,
    ) -> Application:
        """Edit an application"""
        path = "/api/v1/applications/{application_id}"
        path = path.replace("{application_id}", path_segment(application_id))
        query: list[tuple[str, str]] = []
        status, payload = await self._transport.request(
            "PUT",
            path,
            query,
            body.to_json(),
        )
        raise_for_status(status, payload)
        return Application.from_json(decode_payload(payload))


class ErrorsAsyncApi:
    """What the API declares under `errors`, issued through the transport it is handed."""

    def __init__(self, transport: Any) -> None:
        self._transport = transport

    async def list(
        self,
    ) -> list[Problem]:
        """List errors"""
        path = "/api/v1/errors/"
        query: list[tuple[str, str]] = []
        status, payload = await self._transport.request(
            "GET",
            path,
            query,
            None,
        )
        raise_for_status(status, payload)
        return as_list(Problem.from_json)(decode_payload(payload))


class EventTypesAsyncApi:
    """What the API declares under `eventTypes`, issued through the transport it is handed."""

    def __init__(self, transport: Any) -> None:
        self._transport = transport

    async def create(
        self,
        body: EventTypePost,
    ) -> EventType:
        """Create a new event type"""
        path = "/api/v1/event_types/"
        query: list[tuple[str, str]] = []
        status, payload = await self._transport.request(
            "POST",
            path,
            query,
            body.to_json(),
        )
        raise_for_status(status, payload)
        return EventType.from_json(decode_payload(payload))

    async def delete(
        self,
        event_type_name: str,
        application_id: str,
    ) -> None:
        """Delete an event type"""
        path = "/api/v1/event_types/{event_type_name}"
        path = path.replace("{event_type_name}", path_segment(event_type_name))
        query: list[tuple[str, str]] = []
        query.append(("application_id", query_value(application_id)))
        status, payload = await self._transport.request(
            "DELETE",
            path,
            query,
            None,
        )
        raise_for_status(status, payload)
        return None

    async def get(
        self,
        event_type_name: str,
        application_id: str,
    ) -> EventType:
        """Get an event type by its name"""
        path = "/api/v1/event_types/{event_type_name}"
        path = path.replace("{event_type_name}", path_segment(event_type_name))
        query: list[tuple[str, str]] = []
        query.append(("application_id", query_value(application_id)))
        status, payload = await self._transport.request(
            "GET",
            path,
            query,
            None,
        )
        raise_for_status(status, payload)
        return EventType.from_json(decode_payload(payload))

    async def list(
        self,
        application_id: str,
    ) -> list[EventType]:
        """List event types"""
        path = "/api/v1/event_types/"
        query: list[tuple[str, str]] = []
        query.append(("application_id", query_value(application_id)))
        status, payload = await self._transport.request(
            "GET",
            path,
            query,
            None,
        )
        raise_for_status(status, payload)
        return as_list(EventType.from_json)(decode_payload(payload))


class EventsAsyncApi:
    """What the API declares under `events`, issued through the transport it is handed."""

    def __init__(self, transport: Any) -> None:
        self._transport = transport

    async def get(
        self,
        event_id: str,
        application_id: str,
    ) -> EventWithPayload:
        """Get an event by its ID"""
        path = "/api/v1/events/{event_id}"
        path = path.replace("{event_id}", path_segment(event_id))
        query: list[tuple[str, str]] = []
        query.append(("application_id", query_value(application_id)))
        status, payload = await self._transport.request(
            "GET",
            path,
            query,
            None,
        )
        raise_for_status(status, payload)
        return EventWithPayload.from_json(decode_payload(payload))

    async def ingest(
        self,
        body: EventPost,
    ) -> IngestedEvent:
        """Ingest an event"""
        path = "/api/v1/event/"
        query: list[tuple[str, str]] = []
        status, payload = await self._transport.request(
            "POST",
            path,
            query,
            body.to_json(),
        )
        raise_for_status(status, payload)
        return IngestedEvent.from_json(decode_payload(payload))

    async def list(
        self,
        application_id: str,
    ) -> list[Event]:
        """List latest events"""
        path = "/api/v1/events/"
        query: list[tuple[str, str]] = []
        query.append(("application_id", query_value(application_id)))
        status, payload = await self._transport.request(
            "GET",
            path,
            query,
            None,
        )
        raise_for_status(status, payload)
        return as_list(Event.from_json)(decode_payload(payload))

    async def replay(
        self,
        event_id: str,
        body: ReplayEvent,
    ) -> None:
        """Replay an event"""
        path = "/api/v1/events/{event_id}/replay"
        path = path.replace("{event_id}", path_segment(event_id))
        query: list[tuple[str, str]] = []
        status, payload = await self._transport.request(
            "POST",
            path,
            query,
            body.to_json(),
        )
        raise_for_status(status, payload)
        return None


class EventsPerDayAsyncApi:
    """What the API declares under `events_per_day`, issued through the transport it is handed."""

    def __init__(self, transport: Any) -> None:
        self._transport = transport

    async def list_for_application(
        self,
        application_id: str,
        from_: str | None = None,
        to: str | None = None,
    ) -> list[EventsPerDayEntry]:
        """List events per day for an application"""
        path = "/api/v1/events_per_day/application"
        query: list[tuple[str, str]] = []
        query.append(("application_id", query_value(application_id)))
        if from_ is not None:
            query.append(("from", query_value(from_)))
        if to is not None:
            query.append(("to", query_value(to)))
        status, payload = await self._transport.request(
            "GET",
            path,
            query,
            None,
        )
        raise_for_status(status, payload)
        return as_list(EventsPerDayEntry.from_json)(decode_payload(payload))

    async def list_for_organization(
        self,
        organization_id: str,
        from_: str | None = None,
        to: str | None = None,
    ) -> list[EventsPerDayEntry]:
        """List events per day for an organization"""
        path = "/api/v1/events_per_day/organization"
        query: list[tuple[str, str]] = []
        query.append(("organization_id", query_value(organization_id)))
        if from_ is not None:
            query.append(("from", query_value(from_)))
        if to is not None:
            query.append(("to", query_value(to)))
        status, payload = await self._transport.request(
            "GET",
            path,
            query,
            None,
        )
        raise_for_status(status, payload)
        return as_list(EventsPerDayEntry.from_json)(decode_payload(payload))


class InstanceAsyncApi:
    """What the API declares under `instance`, issued through the transport it is handed."""

    def __init__(self, transport: Any) -> None:
        self._transport = transport

    async def get(
        self,
    ) -> InstanceConfig:
        """Get instance configuration"""
        path = "/api/v1/instance/"
        query: list[tuple[str, str]] = []
        status, payload = await self._transport.request(
            "GET",
            path,
            query,
            None,
        )
        raise_for_status(status, payload)
        return InstanceConfig.from_json(decode_payload(payload))


class PayloadContentTypesAsyncApi:
    """What the API declares under `payload_content_types`, issued through the transport it is handed."""

    def __init__(self, transport: Any) -> None:
        self._transport = transport

    async def list(
        self,
    ) -> list[str]:
        """List supported event payload content types"""
        path = "/api/v1/payload_content_types/"
        query: list[tuple[str, str]] = []
        status, payload = await self._transport.request(
            "GET",
            path,
            query,
            None,
        )
        raise_for_status(status, payload)
        return as_list(as_text)(decode_payload(payload))


class QuotasAsyncApi:
    """What the API declares under `quotas`, issued through the transport it is handed."""

    def __init__(self, transport: Any) -> None:
        self._transport = transport

    async def get(
        self,
    ) -> QuotasResponse:
        """Get quotas"""
        path = "/api/v1/quotas/"
        query: list[tuple[str, str]] = []
        status, payload = await self._transport.request(
            "GET",
            path,
            query,
            None,
        )
        raise_for_status(status, payload)
        return QuotasResponse.from_json(decode_payload(payload))


class RequestAttemptsAsyncApi:
    """What the API declares under `requestAttempts`, issued through the transport it is handed."""

    def __init__(self, transport: Any) -> None:
        self._transport = transport

    async def get(
        self,
        request_attempt_id: str,
        application_id: str,
    ) -> RequestAttempt:
        """Get a request attempt by its ID"""
        path = "/api/v1/request_attempts/{request_attempt_id}"
        path = path.replace("{request_attempt_id}", path_segment(request_attempt_id))
        query: list[tuple[str, str]] = []
        query.append(("application_id", query_value(application_id)))
        status, payload = await self._transport.request(
            "GET",
            path,
            query,
            None,
        )
        raise_for_status(status, payload)
        return RequestAttempt.from_json(decode_payload(payload))

    async def read(
        self,
        application_id: str,
        event_event_type_names: str | None = None,
        event_id: str | None = None,
        max_created_at: str | None = None,
        min_created_at: str | None = None,
        pagination_cursor: str | None = None,
        subscription_id: str | None = None,
    ) -> list[RequestAttempt]:
        """List request attempts"""
        path = "/api/v1/request_attempts/"
        query: list[tuple[str, str]] = []
        query.append(("application_id", query_value(application_id)))
        if event_event_type_names is not None:
            query.append(("event.event_type_names", query_value(event_event_type_names)))
        if event_id is not None:
            query.append(("event_id", query_value(event_id)))
        if max_created_at is not None:
            query.append(("max_created_at", query_value(max_created_at)))
        if min_created_at is not None:
            query.append(("min_created_at", query_value(min_created_at)))
        if pagination_cursor is not None:
            query.append(("pagination_cursor", query_value(pagination_cursor)))
        if subscription_id is not None:
            query.append(("subscription_id", query_value(subscription_id)))
        status, payload = await self._transport.request(
            "GET",
            path,
            query,
            None,
        )
        raise_for_status(status, payload)
        return as_list(RequestAttempt.from_json)(decode_payload(payload))


class ResponseAsyncApi:
    """What the API declares under `response`, issued through the transport it is handed."""

    def __init__(self, transport: Any) -> None:
        self._transport = transport

    async def get(
        self,
        response_id: str,
        application_id: str,
    ) -> Response:
        """Get a response by its ID"""
        path = "/api/v1/responses/{response_id}"
        path = path.replace("{response_id}", path_segment(response_id))
        query: list[tuple[str, str]] = []
        query.append(("application_id", query_value(application_id)))
        status, payload = await self._transport.request(
            "GET",
            path,
            query,
            None,
        )
        raise_for_status(status, payload)
        return Response.from_json(decode_payload(payload))


class ServiceTokenAsyncApi:
    """What the API declares under `serviceToken`, issued through the transport it is handed."""

    def __init__(self, transport: Any) -> None:
        self._transport = transport

    async def create(
        self,
        body: ServiceTokenPost,
    ) -> ServiceToken:
        """Create a new service token"""
        path = "/api/v1/service_token/"
        query: list[tuple[str, str]] = []
        status, payload = await self._transport.request(
            "POST",
            path,
            query,
            body.to_json(),
        )
        raise_for_status(status, payload)
        return ServiceToken.from_json(decode_payload(payload))

    async def delete(
        self,
        service_token_id: str,
        organization_id: str,
    ) -> None:
        """Delete a service token"""
        path = "/api/v1/service_token/{service_token_id}"
        path = path.replace("{service_token_id}", path_segment(service_token_id))
        query: list[tuple[str, str]] = []
        query.append(("organization_id", query_value(organization_id)))
        status, payload = await self._transport.request(
            "DELETE",
            path,
            query,
            None,
        )
        raise_for_status(status, payload)
        return None

    async def edit(
        self,
        service_token_id: str,
        body: ServiceTokenPost,
    ) -> ServiceToken:
        """Edit a service token"""
        path = "/api/v1/service_token/{service_token_id}"
        path = path.replace("{service_token_id}", path_segment(service_token_id))
        query: list[tuple[str, str]] = []
        status, payload = await self._transport.request(
            "PUT",
            path,
            query,
            body.to_json(),
        )
        raise_for_status(status, payload)
        return ServiceToken.from_json(decode_payload(payload))

    async def get(
        self,
        service_token_id: str,
        organization_id: str,
    ) -> ServiceToken:
        """Get a service token"""
        path = "/api/v1/service_token/{service_token_id}"
        path = path.replace("{service_token_id}", path_segment(service_token_id))
        query: list[tuple[str, str]] = []
        query.append(("organization_id", query_value(organization_id)))
        status, payload = await self._transport.request(
            "GET",
            path,
            query,
            None,
        )
        raise_for_status(status, payload)
        return ServiceToken.from_json(decode_payload(payload))

    async def list(
        self,
        organization_id: str,
    ) -> list[ServiceToken]:
        """List service tokens"""
        path = "/api/v1/service_token/"
        query: list[tuple[str, str]] = []
        query.append(("organization_id", query_value(organization_id)))
        status, payload = await self._transport.request(
            "GET",
            path,
            query,
            None,
        )
        raise_for_status(status, payload)
        return as_list(ServiceToken.from_json)(decode_payload(payload))


class SubscriptionsAsyncApi:
    """What the API declares under `subscriptions`, issued through the transport it is handed."""

    def __init__(self, transport: Any) -> None:
        self._transport = transport

    async def create(
        self,
        body: SubscriptionPost,
    ) -> Subscription:
        """Create a new subscription"""
        path = "/api/v1/subscriptions/"
        query: list[tuple[str, str]] = []
        status, payload = await self._transport.request(
            "POST",
            path,
            query,
            body.to_json(),
        )
        raise_for_status(status, payload)
        return Subscription.from_json(decode_payload(payload))

    async def delete(
        self,
        subscription_id: str,
        application_id: str,
    ) -> None:
        """Delete a subscription"""
        path = "/api/v1/subscriptions/{subscription_id}"
        path = path.replace("{subscription_id}", path_segment(subscription_id))
        query: list[tuple[str, str]] = []
        query.append(("application_id", query_value(application_id)))
        status, payload = await self._transport.request(
            "DELETE",
            path,
            query,
            None,
        )
        raise_for_status(status, payload)
        return None

    async def get(
        self,
        subscription_id: str,
    ) -> Subscription:
        """Get a subscription by its ID"""
        path = "/api/v1/subscriptions/{subscription_id}"
        path = path.replace("{subscription_id}", path_segment(subscription_id))
        query: list[tuple[str, str]] = []
        status, payload = await self._transport.request(
            "GET",
            path,
            query,
            None,
        )
        raise_for_status(status, payload)
        return Subscription.from_json(decode_payload(payload))

    async def list(
        self,
        application_id: str,
    ) -> list[Subscription]:
        """List subscriptions"""
        path = "/api/v1/subscriptions/"
        query: list[tuple[str, str]] = []
        query.append(("application_id", query_value(application_id)))
        status, payload = await self._transport.request(
            "GET",
            path,
            query,
            None,
        )
        raise_for_status(status, payload)
        return as_list(Subscription.from_json)(decode_payload(payload))

    async def update(
        self,
        subscription_id: str,
        body: SubscriptionPost,
    ) -> Subscription:
        """Update a subscription"""
        path = "/api/v1/subscriptions/{subscription_id}"
        path = path.replace("{subscription_id}", path_segment(subscription_id))
        query: list[tuple[str, str]] = []
        status, payload = await self._transport.request(
            "PUT",
            path,
            query,
            body.to_json(),
        )
        raise_for_status(status, payload)
        return Subscription.from_json(decode_payload(payload))
