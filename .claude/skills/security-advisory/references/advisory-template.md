# GitHub Security Advisory template

Blank fields to fill when drafting an advisory in the GitHub editor. Published
advisories live on the public
[security advisories page](../../../../documentation/resources/security-advisories.md),
not here.

---

**Title**

<what the flaw is, plainly>

**Ecosystem / package**

Other: Hook0 <component> (`github.com/hook0/hook0`). Affects both
`app.hook0.com` and self-hosted deployments, which run the same code.

**Affected versions**

Builds before the fix commit. Self-hosted deployments build from the repository,
so the boundary is the commit rather than a package version.

**Patched versions**

The fix commit onward.

**Severity (CVSS v3.1)**

<vector; the editor computes the score from the metrics>

**Weakness (CWE)**

<CWE id and name>

**Description**

<what was wrong, the impact, and the fix. Write it the way the disclosure policy
and acknowledgments read: what the reader can act on, no ticket numbers, no
internal tooling.>

**Impact**

<who can do what>

**Credits**

<reporter name, spelled as they gave it, unless they asked to stay off>
