apiVersion: v1
kind: Service
metadata:
  name: {{ include "hook0-play.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "hook0-play.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "hook0-play.selectorLabels" . | nindent 4 }}
