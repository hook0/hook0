frontend.build:
  stage: build
  dependencies: []
  rules:
    # Always run on release tags
    - if: $CI_COMMIT_TAG =~ /^v\d+\.\d+\.\d+$/
    - changes:
        - frontend/**/*
  image: node:$NODE_VERSION
  script:
    - pushd frontend/
    - npm ci
    - npm run build
  artifacts:
    name: "$CI_BUILD_NAME"
    paths:
      - frontend/node_modules/
      - frontend/dist/

frontend.check:
  stage: test
  dependencies:
    - frontend.build
  rules:
    # Always run on release tags
    - if: $CI_COMMIT_TAG =~ /^v\d+\.\d+\.\d+$/
    - changes:
        - frontend/**/*
  image: node:$NODE_VERSION
  script:
    - pushd frontend/
    - npm run check
    - npm run lint
