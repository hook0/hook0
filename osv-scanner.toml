# Configuration for github.com/google/osv-scanner
#
# Documented ignores for advisories where a version bump is not viable today.
# Each entry MUST include a reason and SHOULD include an ignoreUntil date so we
# get reminded to re-evaluate. When the underlying crate gets a fix or a viable
# replacement, drop the corresponding entry.

[[IgnoredVulns]]
id = "RUSTSEC-2024-0436"
ignoreUntil = "2026-12-31T00:00:00Z"
reason = """
paste 1.0.15 is unmaintained but only pulled transitively by
tikv-jemalloc-ctl (jemalloc feature on hook0-api) and pprof_util
(profiling feature, disabled by default). No first-party code uses
paste. Migration to pastey (drop-in fork) depends on tikv-jemallocator
upstream — tracked at https://github.com/tikv/jemallocator/issues
"""

# Two migrations this file was waiting on have landed, and their entries are gone
# with the crates that needed them: `validator` is at 0.21.0, which dropped the
# five `unic-*` crates (RUSTSEC-2025-0075, -0080, -0081, -0098, -0101), and
# `hickory-proto` is at 0.26.1, which is the bump RUSTSEC-2026-0118 and -0119
# were deferred against. `rsa` (RUSTSEC-2023-0071) came in through `sqlx-mysql`
# and is no longer in `Cargo.lock` at all. Each was removed after checking the
# lockfile rather than after reading the scanner's "unused ignores" list, which
# in v2.3.0 names every entry whether or not it is doing anything — see the note
# at the bottom of this file.

# Rust transitive bumps deferred to a follow-up MR (require an upgrade of
# aws-sdk-s3 to a version that pulls rustls 0.23+). Tracked separately.

[[IgnoredVulns]]
id = "RUSTSEC-2026-0098"
ignoreUntil = "2026-08-31T00:00:00Z"
reason = "rustls-webpki 0.101.7 → 0.103.x deferred to follow-up MR (needs aws-sdk-s3 → rustls 0.23). Tracked."

[[IgnoredVulns]]
id = "RUSTSEC-2026-0099"
ignoreUntil = "2026-08-31T00:00:00Z"
reason = "rustls-webpki 0.101.7 → 0.103.x deferred to follow-up MR (needs aws-sdk-s3 → rustls 0.23). Tracked."

[[IgnoredVulns]]
id = "RUSTSEC-2026-0104"
ignoreUntil = "2026-08-31T00:00:00Z"
reason = "rustls-webpki 0.101.7 → 0.103.x deferred to follow-up MR (needs aws-sdk-s3 → rustls 0.23). Tracked."

[[IgnoredVulns]]
id = "RUSTSEC-2026-0173"
ignoreUntil = "2026-12-31T00:00:00Z"
reason = """
proc-macro-error2 2.0.1 is an "unmaintained" informational advisory (no code
vulnerability, patched = []). It is a compile-time proc-macro dependency only,
pulled transitively by biscuit-quote (biscuit-auth) and paperclip-macros
(paperclip); no proc-macro-error2 code ships in the running binary. No fix
version exists; removal depends on biscuit-auth and paperclip upstreams
migrating off it. Tracked.
"""

# image-size 2.0.2 is the latest published version (dist-tags.latest), and both
# advisories carry no fixed version — there is nothing to bump to. Re-evaluate
# on the ignoreUntil date, or as soon as image-size ships a release.

[[IgnoredVulns]]
id = "GHSA-5p2g-fcmc-qvqq"
ignoreUntil = "2026-11-30T00:00:00Z"
reason = """
image-size 2.0.2: denial of service through infinite loops in the JXL and HEIF
parsers. Pulled transitively by @docusaurus/mdx-loader, which measures the
images committed in documentation/ at build time. The parser never sees input
we do not control, and it runs in CI, not at runtime — a hang would fail the
docs build, not affect a user. No fixed version exists: 2.0.2 is the latest
release upstream.
"""

[[IgnoredVulns]]
id = "GHSA-w3rx-r6r6-pgpr"
ignoreUntil = "2026-11-30T00:00:00Z"
reason = """
image-size 2.0.2: denial of service through an infinite loop in the ICNS
parser. Same exposure as GHSA-5p2g-fcmc-qvqq — @docusaurus/mdx-loader parsing
our own documentation images at build time, in CI, with no fixed version
available upstream.
"""

[[IgnoredVulns]]
id = "RUSTSEC-2026-0253"
ignoreUntil = "2026-08-31T00:00:00Z"
reason = """
lru 0.16.4 is pulled only by aws-sdk-s3, which pins lru ^0.16.3 up to and
including its latest release (1.141.0) — no published aws-sdk-s3 moves to the
fixed 0.18.x line, so there is no bump available today. The fixed line is
already vendored in the tree at 0.18.2 (via ratatui-core), so only the
aws-sdk-s3 copy is affected.

The advisory needs a panic to unwind out of a key's Drop while inside
LruCache::pop(), caught by std::panic::catch_unwind — the workspace contains no
catch_unwind at all, so the unsound state cannot be observed here. Re-evaluated
with the other aws-sdk-s3-blocked entries on 2026-08-31.
"""

# Do not prune this file from the scanner's "unused ignores" list.
#
# In osv-scanner v2.3.0 that list names every entry, working or not. `ShouldIgnore`
# hands back a copy of the entry (internal/config/config.go), the filter calls
# `MarkAsUsed()` on that copy (pkg/osvscanner/filter.go), and the copy is dropped
# on the next line — so the flag the list reads is never set on the entry the
# config holds. A run that filters eleven findings still reports all of its
# ignores as unused, which is what this repository's own output shows.
#
# What does say an entry is live is the `<id> has been filtered out because:` line
# the same run prints, and what says one is dead is the crate being absent from
# the lockfile. Both were checked before anything was removed here.
#
# The list is also harmless: it is logged at warning level, and only an
# error-level log sets the exit code to 127.
